# Self-growth candidate queue. Only status=ready candidates can be promoted.
- entry:
    id: xgboost
    method: XGBoost (prediction)
    domain: machine learning
    family: ml
    goal: predict
    description: Gradient-boosted trees for regression (continuous outcome) or classification (binary outcome), with feature importances.
    preconditions:
      min_rows: 50
    produces:
      - test-set score
      - feature importances
    executor_ref: xgboost
    biases:
      - not causal (predictive only)
      - overfitting if untuned
  source: manual (self-growth demo)
  status: ready
  notes: executor wired (mirrors random_forest); tests + Opus double-review LGTM
