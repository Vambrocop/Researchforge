# 未做 / 降级 / 待优化 日志（Deferred & Optimization Log）

> 这里记录**没做完、降级处理、或受环境限制绕过**的事项，方便后续回头优化。
> 每条：**项** · **为何没做/降级** · **当前可用替代/现状** · **如何补全/优化**。
> 由 AI 自走时持续追加（受 CPU/GPU/装包/后端限制时尤其要写在这）。

## 🔜 下一波（优先级，2026-06-19 更新）

> 新会话/下一波接着做这些（记忆 `next-batch` 也指向这里）。**已完成（别重做）**：run.py 巨石全拆、测试提速（`pytest -n 2`；别用 `-n auto` OOM）、conformal、**因果/计量方法波**（PSM/IPW/event_study/fuzzy_rdd/staggered_did）、**推断 backlog #4a-#4d**（causal_forest BH/FDR、BART holdout R²、DML nuisance CV R²、GAMM 非高斯族）、**A 风格 web 前端**（接引擎 + goals 后端供给）、**时间序列波**（协整+VECM/GARCH/结构突变/STL/ARDL）、**并行波1**（农学 AMMI/GGE/RSM + 空间面板 SAR/SEM/SDM + 生态 Mantel/IndVal/RDA）、**DesignSync**（组件库推 claude.ai/design "ResearchForge UI"）、**并行波2**（生存 competing_risks/parametric_survival/rmst + MCDA entropy_weight/vikor/promethee/ahp + 多元 manova/discriminant/canonical_correlation/hotelling_t2）、**并行波3**（心理测量 cronbach_alpha/mcdonald_omega/icc + 金融 value_at_risk/extreme_value/risk_adjusted_return + 非参 permutation_test/bootstrap_ci/robust_regression + 状态空间 unobserved_components/markov_switching/dynamic_factor）+ **引擎审计**（只读,无正确性 bug/无死代码,注册表↔catalog 100 id 双向对齐）、**并行波4**（计数 zero_inflated_poisson/zero_inflated_negbin/tweedie_glm + GWR 地理加权回归 + 贝叶斯共轭 bayesian_ab_test/bayesian_proportion/bayesian_poisson_rate + 一致性 cohens_kappa/fleiss_kappa/bland_altman）、**并行波5**（装 girth/stepmix/mirt/did/poLCA：IRT irt_2pl/irt_rasch + 潜类别 latent_class_analysis/latent_profile_analysis + 混合/异常 gaussian_mixture/dbscan_clustering/mahalanobis_outliers + Callaway-Sant'Anna 交错 DiD callaway_santanna）、**并行波①**（装 prince/python-louvain：因果敏感性 oster_delta/evalue + 多项 IRT irt_grm/irt_pcm/dif_detection + 降维 mds/correspondence_analysis/pls_regression + 网络科学 community_detection/centrality_suite/epidemic_model）、**波②**（列联表/分布拟合/调节过程 + rosenbaum 重做）、**波③**（现代 DiD/生存补全/面板补全）、**波④**（空间补全 ripleys_k/getis_ord/join_count + 回归诊断 vif/heteroskedasticity/influence + 相对重要性 dominance/relative_weights/commonality + `causal.py`→`branches/causal/` 提包）、**波⑤**（有序回归 proportional_odds_logit/ordered_probit/brant_test + 密度尾部 finite_mixture/kernel_density/tail_index + 抽样设计 weighted_estimation/poststratification/raking + **v0.8 里程碑达成**：Web MVP scorecard 探测修正→可用性83/总分93 + /api/run 接 config + 版本 v0.7→v0.8）、**波⑥**（经典 ANOVA anova_oneway/ancova/repeated_measures_anova + 非参组检验 kruskal_wallis/friedman_test/mann_whitney + 监督 ML regularized_regression/svm_model/gradient_boosting）。全部 inference-reviewer 双审或确定性实测（波5 主脑亲审；波①–⑥ 主脑兜底 + 冷启动 reviewer 双重确认）。**全量 809 绿（189 方法）· 引擎 v0.8.0**。

0. ✅ **估计量正确性深审波（2026-07-03）·22 族逐方法对照金标准 → 9 修复（4 HIGH+7 MED）**：用户"深审波·续昨天"——对昨天只做反模式扫描、估计量数学没逐一验的 ~22 族，派 `inference-reviewer` 专才**逐方法对照权威参考 + 数值探针**(lifelines/statsmodels/scipy/sklearn/R DescTools·ChainLadder·spdep·gstat)验公式/边界/披露。全量 `-n 2` **1456 绿 / 5 skip / 0 fail**（基线 1428→+28 新测）。9 修复各独立本地 commit，**未推**。
   - **核心结论**：**绝大多数估计量数学本身是对的**（DeLong AUC 方差/Mack σ²/BCa 逐位对 scipy/split-plot 双误差项/Hausman 差协方差/gage R&R EMS/Moran·Gi*·GWR/Hotelling·CCA·Shapley/阈值反变换/J-N/relative-weights SVD/rosenbaum/e-value 全部对金标准验过没问题）。bug 集中在**尾部处理·方差分母·列解析接线·披露诚实**——多个通过了 build 时审查但深审才逮到。
   - **9 修复**：**HIGH** `3233bf1` spc `_measurement_col` 按 profiler 顺序取首个 continuous/count/id → 标准布局[id..,测量]下整数 id 列被抢(Cp 0.17 vs 1.16、控制图 71/75 失控)；`871b5cd` actuarial TVaR 发散尾(Pareto ξ<1)quad 返回大负有限数、诚实门抓不住→打印负 TVaR（**+**同 commit MED Mack SE 用整列和含对角元→SE 偏小，改 Mack1993 因子分母，RAA 复现 R 26909.01）；`34ff28e` distribution AD `weibull_min` scipy 用 1−α 置信度表[0.5..0.995]、代码当小-α 取最近 0.05→落 0.5 分位(crit 0.342 vs 0.758)过度拒绝；`2246aeb` categorical Goodman-Kruskal γ SE 常数 4→2(每个 CI 宽一倍)。**MED** `169e3ce` epidemiology 字符串标签列阳性档静默倒置→摘要披露所取档；`bd92045` timeseries 协整秩检验(det_order=0=co)与 VECM(ci)设定不一致→统一 co；`e5c2c37` reliability_growth ⚠/code 把默认说成时间截尾(实为失效截尾)；`715ea1d` Tobit E[y]边际尺度因子漏删失点 c；`2246aeb` fisher OR 标『条件MLE』实为样本 ad/bc；`ccdda03` econometrics random_effects 不过滤时不变列(AbsorbingEffectError 挂)+ Hausman 无条件 pinv+clip 非PSD时伪推荐RE（复用姊妹 hausman_test 的 eigmin>0 守卫+回归式回退）。
   - **CLEAN(仅 LOW→backlog)**：multivariate·game_theory·spatial(13法)·survival·survival_extra Cox·agreement+effect_sizes·nonparametric·experimental(11法,split-plot 双误差项对)·count_models·ordinal·forecasting+finance·sensitivity+importance·reg_diag+interpretability·mediation(5法)——这些族逐方法验过、无 must-fix。
   - **⚠ LOW backlog(各族审计报告里，未逐一落，择要)**：multivariate LDA 单折CV无显著性检验·Hotelling inv 无近奇异守卫；epidemiology RD 零格 Wald 反保守；spatial join-count 自由抽样偏乐观·LISA 多重比较未披露；nonparametric KW 静默剔 <2 组·MWU HL-CI 近似标签；ordinal estimates 键碰撞(预测变量名=n/loglik)·Brant 近似协方差保守；mediation serial/parallel 缺自动角色警告+常数列守卫·跨文件 BCa vs percentile 不一致；interpretability 分类 PDP 对数几率尺度 vs ALE 概率尺度未披露·ICE docstring 名不副实·surrogate 样本内保真；forecasting ETS 两次拟合参数错配·EVT KS 插入参数 p 偏乐观；reg_diag 全 clean；e-value 连续暴露中位分割未披露；survival_extra Efron 未标+EPV 软警告；oster_delta 线性近似(已披露)。
   - **⏭ 未审(Batch2c，低危)**：latent_class·mixture·psychometrics·survey_methods·choice·missing_data(多为 build 时已 inference-review)·domain(hydrology/soil/field_trials)·确定性计算器(mcda/operations_research/resource_*)。
   - **教训**：① build 时审查会漏——estimator 通过恢复测试不代表辅助量(TVaR 尾/γ SE/Tobit 边际尺度)对，深审逐方法对金标准才逮到；② 常见坑类型固化：尾部发散未定义(TVaR/EVT)、方差分母(Mack S_j/γ SE 常数)、列解析被 id-trap 抢(spc，同 profiler id-trap 未集中化根因)、临界值表约定(AD weibull 1−α vs α)、字符串标签阳性档倒置、检验/模型设定项不一致(协整 co/ci)；③ 外部权威(R DescTools/ChainLadder/spdep + scipy 探针)验证 > 纯读码，reviewer 都跑了数值核对。

0. ✅ **Sonnet5+Fable5 双模型全审 → Fable 主脑分配 → 14 项修复波（2026-07-02）**：用户"用 sonnet 5 和 fable 5 分别审核完整代码、比较、再优化" → "fable 5 当主脑分配任务把 P0–P3 做了"。全量 `-n 2` **1423 绿 / 5 skip / 0 fail**（基线 1381 → +42 新测）。14 项各独立本地 commit，**未推**。
   - **双审对比**：Sonnet 5 + Fable 5 各独立冷审全码(同一 brief)，主脑(Opus)亲验 top 发现再合并。**收敛=高信心**：profiler 二值{1,2}误判、边门禁松于 affinity、web 上传上限、savefig 语言冻结——两模型都命中。**Sonnet 更广**(fan-out 子代理)：多挖 3 个 R 列名守卫漏洞(ecology ALDEx2 / meta es_cols / panel QCA unit) + IRT PCM 模型族错配 + 引擎韧性 + statistics ANOVA；**Fable 抽样 R 站点后过度泛化成「全有守卫」，漏了那 3 个**。**Fable 更细**：多挖 roles 裸字母正则、config 措辞过度承诺。
   - **Fable 5 当主脑分配**：喂它全部已验证发现 → 它出**任务分配方案**(测序 / STOP 判断点 / model-tier / reviewer≠builder / commit 信息)；Opus 当手执行(派 Sonnet/Haiku 建、Opus 冷审、commit 权+亲验留主脑)。分层委派省 token + 保审建分离。
   - **14 项修复**：**P0** `c3fb60e` 两值整数→binary(~15 分支恢复) · `fde2db0` 集中化 R 列名守卫 rbridge.r_names_safe+补 3 漏网点 · `7191ee6` requires_edgelist 对齐 affinity 高基数阈值。**P1** `bffffe9` irt_pcm 换 GPCM 相邻类别 θ 打分器(inference-reviewer 裁 CORRECT，并逮到 girth NaN 步→除总归一污染全体 θ 的 SHOULD-FIX，已修掩蔽+披露)。**P2** `1b88521` branches 自动发现单模块失败隔离 · `a1ebd14` run 分发点 try/except 出降级报告 · `8c72ecf` web 上传分块上限前置+文件夹数量/总量上限 · `efdeeba` savefig 本地化逐次读 RF_FIG_LANG/CJK。**P3** `c9dd69a` ecology PERMANOVA/Mantel(z-score)/NMDS(Stress-1)/β多样性(空样点) 披露与度量修正+ALDEx2 种子 · `5a9b91b` ETS DIF 非显著项一律标 A · `546731a` group_comparison ≥2/组守卫+方差齐性披露 · `9e8a73a` PSM ATT p 值改 t 分布 · `09a8846` roles 去单字母+config 措辞 · `1e2a065` time_col 需名称锚点。
   - **⚠ 待决/backlog（记此，无条件）**：① **P3-4 Welch 切换（实质决策，待拍板）**——k≥3 是否从 f_oneway 改 Welch/Brown-Forsythe ANOVA(builder 建议切:与 k=2 的 Welch 一致；改结果，故先只做「守卫+披露」安全版，切换留待终审/用户)；② **ecology.py 1216 行**越过 ~1200 软提包线(新 canary，下次加东西前提包 `branches/ecology/`)；③ Sonnet 全审「未覆盖」：~40 个 family 仅反模式扫描、估计量数学未逐一验(spc/actuarial/game_theory/reliability/techno_economic/marketing/epidemiology/text_mining/spatial…)，后续可续审。
   - **教训**：① 冷审抽样会过度泛化(Fable 判「R 站点全有守卫」漏 3 个)——广度(Sonnet fan-out)+主脑亲验补上；② inference-reviewer 逮到 builder+主脑都没想到的 NaN 除总污染(对抗性审推断的价值)；③ 守卫惯例手抄必漂移→集中化(rbridge.r_names_safe)是根治；④ 子代理会撞会话额度中断，重试/换时段可恢复；⑤ builder 误把 pytest 输出重定向出 `pcmlog` 垃圾文件——提交前 git status 亲查、别误提交。
   - **Fable 5 终审（APPROVED，2026-07-02）**：14 项全签字、无阻塞；肯定 inference-reviewer 逮 P1-1 NaN 除总污染 + P0-2 补回 Fable 自己原审漏的 3 个 R 站点。**P0-1 STOP 门确认未触发**(builder 审了仓库所有 count/binary/continuous 断言，无一依赖两值整数列、零测试被翻)。裁定 **P3-4 = 无条件切 Welch's ANOVA**(见 loop-decisions)。**3 个非阻塞残留记此**：① P1-1 中间类别缺失(person/item 跳过中间类别而非仅顶类别)仅靠诚实分支披露、非完整重参数化——筛查工具下可接受、已披露；② P0-1 边缘：极小样本里「真计数」的两值整数(如 {0,3})现判 binary(正确取舍：两值整数是 binary 远多于 count，已记);③ P3 数值型修正(Mantel z-score / PSM t 分布 / NMDS Stress-1)按设计改了报告值，测试已更新为**修正后**值(非放宽)，bisect 时留意。

0. ✅ **网页增强波 + Sonnet 全审（2026-06-27，接网页审视之后）**：用户"网页给我看看,搜索/筛选/数据类型/文件夹做好了么" + "切到 sonnet 帮我完整审核代码和设计思路"。
   - **网页增强 4 件**（后端 `8c5712f` + 前端 `39f3284`,本地未推）：① **修 Excel 上传真 bug**（上传一律存 .csv → read_table 按后缀判 Excel 失效 → xlsx 被当 CSV 解析崩;改 `_save_upload` 保留原扩展名 + `_resolve_upload` 按 id glob 找,clean/reanalyze/run 都走它）;② **文件夹批量分析** `/api/analyze_folder`（webkitdirectory 传整夹 → 每表 profile+top3 汇总表,非表跳过,每行 file_id 可「打开→」进完整流程;service.analyze_folder_files 不抛错、坏表记 {ok:false}）;③ **方法搜索框**（名/id/族/说明实时过滤）;④ **族 + 严谨度灯筛选**（目标 chips 之外加族下拉 + 🟢🟡🔴 chips,四筛叠加）。test_web 21 绿 + 全量 1379 绿。⚠ **前端 JS/视觉需用户浏览器验**（主脑无浏览器）。
   - **Sonnet 全审（general-purpose model=sonnet,审者≠建者,审设计+代码）**：设计判「架构扎实」（注册表分发 + 自动发现 + ≤1500 护栏 + 分层推荐 + golden 可证伪）。**逮 2 个真 bug（已修 `42c33da`,主脑亲验）**:① **MUST 评分卡族名过期**——`scoring._FAMILY` 用旧名（timeseries≠time-series、panel≠econometrics + 死键 microbiology/qualitative）→ **165/294 法（56%,含 bayesian/survival/time-series/econometrics/regression）静默落 _DEFAULT 通用分**;改对齐 catalog 族名 + 补齐全 45 族 + `test_scoring` 加覆盖断言锁死（arima pop 50→70、survival 50→72、bayesian pub 56→80）。② **SHOULD has_edgelist 过触发**——两个低基数分类列(region+sector)被判边表 → 结构地板把网络方法顶到普通数据 top;改 edge-node 分类列须高基数(n_unique≥12) + 加负向 golden（two_categorical 断言网络方法**不**在 top6,已验通过 → 现出 group_comparison/mixed_effects）。③④ **SHOULD web 安全**:上传 100MB 上限(413,文件夹跳过超大)+ 下载临时 zip 用 BackgroundTask 清理(原每次下载泄漏)。
   - **⚠ Sonnet 审计留的 NICE backlog（未做,记此）**:① `_REGRESSION` 集从 run.py 被 branches/regression.py 反向导入（违"branches 导入 run"约定,当前初始化序 ok 但脆,可移到 _branch_api）;② web `_files` dict + `web_uploads/` 无清理（长跑内存/磁盘涨,单用户无害;可加 TTL 清理命令）;③ `_trend_snapshot` lru_cache 进程级 → `discover --live` 同进程后陈旧;④ `Ctx` 非 frozen（"mutate 别 rebind"约定无强制,footgun）;⑤ savefig monkeypatch 对"作为库导入"的用户是全局副作用（CLI/web 无害）;⑥ `network_science.py` 1232 行、`r_backends.py` 1200 行逼近 1500,下次加东西前拆;⑦ 图标签精确撞词（列名恰好=glossary 词）本地化层固有限制,已披露无测试;⑧ list_runs O(n) 每次扫 outputs（大量 run 时慢）。
   - **教训**:① Sonnet 冷审（换模型+新上下文）逮到主脑盲区——评分卡族名过期潜藏很久（affinity 有覆盖测试、scoring 没有 → 加上）;② 一个子系统有覆盖测试不代表姊妹子系统也有（affinity vs scoring 两个族表,只测了一个）;③ 审计发现的 bug 主脑必亲验再修（本轮 2 个都跑数据确认 165/294、region+sector→网络）。

0. ✅ **角色感知选模 + 冷代码审计（2026-06-27，接图标签/pick 之后）**：用户"二值结果角色检测、ols 错族 可以继续做 + 完整 review 所有代码是否有问题"。
   - **二值结果角色检测**（`5048cc0`）：roles.py `_BIN_OUTCOME_RE` 检测二值分类目标（approved/churn/died/...），先于连续名匹配——真二值目标(approved)击败名字撞车的连续预测变量(score)；高精度(gender 不误判、group 仍是组)；二值目标排除出 treatment。affinity 加 `has_group`(非结果的二值/分类列)；scoring `requires_group` 用 has_group + 恢复满权重(撤 10→5 临时档)。修了排序**和**结果提示/默认。**ols 错族经查非 bug**：econometrics 本就混装面板(FE/RE/GMM)+截面(ols/tobit/truncated/heckman),structure=any + is_panel bonus 是对的设计。全量 1374 绿。
   - **冷代码审计**（agent-skills:code-reviewer，审者≠建者，审聪明选模 arc + 图标签 + pick + roles）：**ZERO MUST-FIX**，验证了结构地板↔red-cap 顺序正确、零运行时联网、本地化作用域真的只限标题/轴/图例。**应用 2 SHOULD + 1 NICE**：① 图重复保存双重翻译(就地修改+savefig 每次跑;某些 glossary 值仍含英文 token 如 rank/level_k 会二次翻译)→ `_localize_figure` 加 per-figure sentinel `_rf_localized` 幂等;② 标识符片段过度翻译(level_k 里的 level、age2 里的 age)→ 词边界排除 `0-9_`(`(?<![A-Za-z0-9_])...(?![A-Za-z0-9_])`)+ 预编译 patterns(顺带解 >512 正则缓存抖动);③ `event` token 从 `_BIN_OUTCOME_RE` 移除(与生存删失指示撞车)。ruff/vulture/radon 干净(复杂度热点皆 flat 顺序函数、信息性)。
   - **⚠ 审计留的 NICE backlog（记此，未做）**：① **图标签精确撞词**——列名恰好等于某英文 glossary 词(如列名就叫 "price"/"count")仍会被翻译(savefig 层无法知该 token 是列名;标识符片段已由边界修复,只剩裸精确撞词)——本地化层固有限制,已在 CLAUDE.md 注明;彻底解需分支不内插裸列名或本地化层拿到列名清单。② **has_edgelist 宽松**(`≥2 个 id/分类列`)——两个真分类列(region+sector)会判成边表 → 结构地板把网络方法在普通数据上抬高;此宽松在 match.py 既有,结构地板放大了它;真修需区分"边表 vs 交叉分类"(结构上同形,难)——可加高基数门槛 + 负向 golden 守卫。③ `_diag_timeseries` 平方序列 Ljung-Box 对趋势序列可能假阳"波动聚集"(非门控于 ADF);非绑定 nudge、已披露,honesty 级。④ `_cmd_pick` 打印的 JSON config 提示未转义列名中的引号(仅打印不执行,无注入)。⑤ test 安装的全局 savefig patch 不卸载(未来若有 test 存图后查英文标签会见中文;目前无此 test)。
   - **教训**：① 就地修改 + 每次 savefig 跑 = 重复保存双重翻译,幂等 sentinel 是标配;② substring 词表翻译的边界要排除 `0-9_` 才不切坏 snake_case/带数字标识符;裸精确撞词是固有限制要披露;③ 冷审(审者≠建者)逮到 builder 看不见的 re-save 双翻译 + 标识符切割,验证了其价值;④ 自动工具(ruff/vulture/radon)只是objective 第一层,逻辑/边界问题靠冷审。

0. ✅ **图标签中文化 + 快速选模 cli pick（2026-06-27，接聪明选模 arc 之后）**：用户"先 push 然后图标签中文化 然后解决如何快速选择方法"。已 push 前 7 条；本批 2 条本地 commit（`a931d54`+`25dda05`）。
   - **图标签中文化**（a931d54）：`_init_mpl_style` monkeypatch `Figure.savefig`（**单一收口**，每分支出图都过它）→ 存图前 `_localize_figure` 用词表 `_FIG_GLOSSARY`（最长短语优先、词边界、不分大小写）把**标题/轴标签/图例**英文→中文（**仅这些角色，绝不动刻度/数据文本**）。~690 处标签 / ~200 文件 **零分支改动**全覆盖。**门控**：仅当系统有 CJK 字体才翻译（headless/CI 无字体→保英文、不豆腐块）；`RF_FIG_LANG=en` 强制英文（论文用）。词表覆盖常用词、未覆盖留英文可增补（`_FIG_GLOSSARY` 加词即可）。教训：`re` 漏 import 被 `_localize_figure` 的 try/except 吞掉→savefig 静默不翻译（加 import 后修复）；test_fig_localization 验词表/就地翻译/savefig 收口。
   - **快速选模 cli pick**（25dda05）：`cli pick <data> [--goal X]` 一发输出**该跑哪个方法**——top-fit 实质方法（跳过 descriptive/correlation 这类常驻助手）+ 严谨度/契合 + 触发的诊断依据（如"检测到生存/时间-事件数据"）+ **精确 run 命令**（建模法且检出 likely_outcome 时带 --config outcome 提示）+ 2-3 个备选。把"翻 294 个方法"变成"跑这个"。
   - **附带修**：`requires_group` precondition bonus 10→5——它是**松信号**（二值/分类列可能就是结果变量本身、非真分组臂），不该让两臂检验(bayesian_ab_test)越过对同样二值数据更对路的回归(logistic)。验证：binary+predictors 的 pick 现返回 logistic；golden two_group 仍过(9/9)。
   - **⚠ 新 backlog（记此）**：**二值结果的角色检测**——`roles.py` 只检测连续/计数 outcome，识别不了"二值列是结果变量"，导致结构上「1 二值 + k 连续」无法区分"二值结果回归(logistic)"vs"分组比较(group_comparison/A-B)"。真解需 roles.py 加二值-outcome 检测 → 喂亲和度做角色感知选择（per-method/role-aware affinity，是 ols 错族、A-B vs logistic 等一类问题的根治）。当前用 requires_group 降权缓解。
0. ✅ **聪明选模纵深·阶段 0–5（2026-06-27，CJK 字体 + 选择大脑可证伪化与真 fit；用户定的分阶段计划）**：用户暂停加方法，转而审视产品（网页/选模/出图/出结果），并给出分阶段指令：**先做便宜确定的胜利，再把"选择大脑"拆成可验证小阶段；深化 fit 前先让"选择正确性"可证伪**。6 阶段各独立 commit、测试全绿再下一步。引擎 294 法不变（这是**推荐层**升级，非方法波）。**全量 1365 绿**。6 条本地 commit（67a7d25→71adaa7），**未推**。
   - **阶段0·CJK 图字体**（67a7d25）：`_helpers/core.py` 的 `_init_mpl_style`（每次 run 的统一出图入口，run_analysis 在分支前调用）加 `_detect_cjk_font`（检测系统中文字体 Noto/Source Han/YaHei/SimHei/PingFang…，**检测优先、不打包 16MB 字体**）→ 把中文字体插入回退链首 + `axes.unicode_minus=False`；无字体优雅回退英文（headless/CI 仍可跑）。**零分支改动**（全走统一入口）。test_cjk_fonts：渲染中文标签断言**无 "Glyph missing" 警告（无豆腐块）**。CLAUDE.md 出图约定已更新。⚠ 遗留：**现有 60+ 分支的图标签仍是英文**——阶段0 只是"解锁"中文能力，是否把存量标签改中文/双语是独立的标签策略决定（未做，英文仍是 headless/CI 最稳默认）。
   - **阶段1·选择可证伪**（3585e74）：`tests/test_golden_selection.py` —— 8 个结构无歧义的小数据集（panel/timeseries/overdispersed-count/binary/survival/two-group/edgelist/geo）+ 期望方法集，断言合适方法进 `select_top` top-6。**基线 3/8 正确**；其余 5 个 feasible 但被埋（logistic ~#8、survival ~#32、network ~#8/#24、kriging/moran ~#8/#9、group_comparison ~#6/7）→ **strict-xfail 棘轮**（阶段4 修好后 XPASS 会强制提升为硬断言，防静默回归）。**这是后续 fit 改动的安全网，reviewer 没强调但用户点出的关键前提。**
   - **阶段2·亲和画像**（4d1dc1d）：`recommender/affinity.py` —— 45 族**数据亲和画像**（理想结构 panel/timeseries/geo/edgelist/cross_section、outcome 类型、需否预测变量、最小样本）+ `data_signals(fp)`（提取结构信号）+ `match_score(signals, family)`（0-100 族级匹配分）。族级粒度（用户定，不逐 294 法）。
   - **阶段3·真 fit**（45de29f）：`scoring.py` 的 `fit = rigor.score`（≈bias 计数，假的）→ **`_affinity_fit` = 族匹配 + 每方法 precondition 特异性 bonus**（方法的专属前提匹配数据结构则加分——logistic 的 requires_binary_outcome、network 的 requires_edgelist、spatial 的 requires_geo…）；red 法 fit 仍封顶于低 rigor 分。**只改 fit 值不改排序**（golden 仍 4 通过/5 xfail，隔离验证）。test_fit_affinity：合适法 fit>通用法。
   - **阶段4·fit 驱动排序**（10fdc89）：`recommend.py` 档位内排序键 `rigor.score`→`score.fit`（+诊断 nudge，rigor 分做 tiebreak）；档位（绿黄红）仍主键不跨。**选择正确率 3/8→9/9**，5 个 xfail 全翻成硬断言 + 加 linear_regression 守卫。**校准（皆有原理非迁就测试）**：① econometrics 族 structure panel→any（它含 ols_regression 这个截面法，panel 惩罚误伤 ols；panel 成员靠 is_panel bonus）；② 从"需变量"的族（categorical/categorical_tests/configurational/nonparametric/distribution）**删除 "none" outcome**（防它们在无变量数据上白蹭"无结果"匹配）；③ **结构地板**：方法的结构定义前提满足（requires_edgelist/geo/is_panel/is_timeseries）则 base 至少给结构匹配级——把困在 "ml" 族（与随机森林同族）的网络方法在边表上托起来。
   - **阶段5·诊断出 GLM**（71adaa7）：`diagnostics.py` 加 3 类值级诊断接入既有 nudge 框架——`_diag_timeseries`（ADF 非平稳→arima；平方序列 Ljung-Box→garch 波动聚集）、`_diag_survival`（时长+事件→生存模型 over ols/logistic）、`_diag_missingness`（缺失>5%→mice）。statsmodels 调用局部 try/except。test_diagnostics_extra：各在对应数据上触发、在普通截面数据上沉默。
   - **⚠ 本arc 遗留（未做 backlog，记此）**：① **ols_regression 错挂 econometrics 族**（用 structure=any 绕过；真修=per-method 亲和覆盖或重新归族）；② 金融法（value_at_risk/extreme_value/risk_adjusted_return，finance 族 structure=timeseries）在**任意**时间序列上排名偏高（非仅金融收益率）——可加"收益率"信号或 per-method 细化；③ two_group→group_comparison 在 #6（通过但边际薄，未来可加 group+continuous 的诊断 nudge 加固）；④ 亲和是**族级**（用户定的范围）——per-method 亲和覆盖是未来细化；⑤ **图标签中文化**（阶段0 已解锁字体，存量 60+ 分支标签改中文/双语是独立标签策略决定，需用户定方向）；⑥ requires_edgelist 前提本身宽松（≥2 个 id/分类列，非网络的双分类数据会误判为边表）——前提定义问题，超本 arc 范围。
   - **教训**：① **可证伪先行**——golden 套件让"3/8→9/9"成为可测的事实而非"手感"，是 reviewer 漏掉、用户点出的关键；② 排序调试是 whack-a-mole（edgelist 连追 3 轮：categorical 误计→survey/nlp 蹭 none→结构地板才解），根因是 ml 族异质（预测 ML + 网络科学同族）→结构地板按"前提"而非"族"托底是更对的抽象；③ 每阶段独立 commit + golden 守门，让大改动安全可回滚；④ 校准要按领域原理（如删 none、econometrics→any），不为过测迁就。

0. ✅ **贝叶斯纵深续波（2026-06-27，bayesian_gam + bayesian_state_space，主脑亲建 + 1 冷审 NO MUST）**：用户"Bayesian GAM / 状态空间贝叶斯 这两个可以做"。两法各落**新文件**——`bayesian_gam.py`（family statistics，频率派 gam/gamm 的贝叶斯版）、`bayesian_state_space.py`（family time-series，频率派 unobserved_components 的贝叶斯版）；`bayesian_mcmc.py` 已 1088 行，**刻意不再扩它**（新法落新文件，pkgutil 自动注册）。复用 bayesian_mcmc 共享助手。引擎 **292→294 法 / 45 族**。
   - **probe-before-write（贝叶斯识别铁律）**：首轮 GAM max R-hat 1.046 + 多次 max-tree-depth，SS（local linear trend）**57 个发散**。诊断=未标准化 y → HalfNormal 新息先验尺度不匹配。**修：标准化 y**（状态/方差/预测回传原尺度）+ target_accept 0.95(GAM)/0.99(SS)→ GAM R-hat 1.018/2 发散、SS 4 发散，二者仍精确恢复真值（GAM 拟合-真值 corr 0.999；SS 水平 corr 1.0、斜率 0.081 vs 0.08）。**教训固化：贝叶斯层级/时序模型先 probe，标准化响应是抑制发散最有效的一招（57→4）**。
   - **bayesian_gam**（贝叶斯惩罚样条 GAM）：每连续预测变量一个平滑 f(x)=截断三次基 (x−knot)_+³（列中心化）+ **非中心化岭先验** u=su·z（su~HalfNormal=平滑参数，数据估计、向直线收缩）——惩罚样条的混合模型表示（Ruppert-Wand-Carroll）。报每平滑偏效应 + 94% 可信带（CSV+图）、平滑 SD/线性斜率、残差 σ、贝叶斯 R²(plug-in)、收敛。最多 4 平滑项；y/预测变量标准化、平滑回传响应单位。
   - **bayesian_state_space**（贝叶斯结构时序）：local_linear_trend（默认，level + 随机游走 slope）/ local_level；状态用**非中心化累积和**（避方差 funnel）；y 标准化；h 步预测=逐后验 draw 前向模拟（区间随步长变宽）。报平滑 level(+slope) + 94% 带、方差成分（obs/level/slope）、预测 + 收敛。无季节项（季节用频率派 unobserved_components）。
   - **冷审（inference-reviewer，审者≠建者）NO MUST-FIX**：reviewer 用 NumPy 镜像数值验证了 LLT 递推（μ_t=μ_{t-1}+δ_{t-1}+η for t≥2）、预测前向模拟顺序（level 用前一斜率再更新斜率）、所有 back-transform（level/forecast ×ysd+ym、slope/σ ×ysd）、GAM 设计满秩可识别、贝叶斯 R²/linear_slope 链式法则。**1 SHOULD + 2 NICE 全已应用**：① SS 初始条件披露（mu0=首期水平、初始漂移 d0 自第 2 步起作用，formula 对 t≥2）；② GAM 偏残差图改用 `np.interp` 对齐到所绘平滑线（原偏残差按观测 x 中心、线按网格中心→常数偏移，cosmetic）；③ GAM linear_slope 注明为**显式线性项系数**（非整条平滑平均斜率）。
   - **未做 / 待办（NICE，记此）**：① 贝叶斯 R² 为后验均值 plug-in（非 Gelman 2019 逐 draw 平均；与既有 bayesian_regression 约定一致、已披露）；② SS 未含季节成分（季节分解走 unobserved_components）；③ GAM 平滑可信带为等尾 94% 非 HDI（已据实说"可信带"）；④ SS 局部线性趋势方差成分在短/平滑序列上弱识别（已披露、靠 target_accept 0.99 抑发散）。
   - **测试**：6 测绿（PyMC NUTS，SLOW，conftest SLOW_MODULES 已加 test_bayesian_gam/test_bayesian_state_space）；GAM 验平滑恢复 sin（grid corr>0.9）、SS 验趋势恢复（level corr>0.95）+ 预测上行 + 区间变宽。ruff + config/size guard 全绿。
   - **下一波候选**：药理剂量反应 EC50/4PL（用户说方法波最后做）；网页真·视觉重塑·迭代2（卡用户渲染反馈）；其余见下方网络波候选。

0. ✅ **网络科学纵深波（2026-06-27，link_prediction + stochastic_block_model + ergm，主脑亲建 + 1 冷审 ZERO MUST/SHOULD）**：用户"新一天继续"。接 next-batch 明日计划的网络科学纵深，3 法落在既有 `network_science.py`（family ml，与 community_detection/centrality_suite/epidemic_model 同文件，复用 `_resolve_edges`/`_build_graph`）。引擎 **292 法/45 族**（无新族）。**编排决策**：3 法改动同一 family 文件 + 同一 yaml → 并行 worktree subagent 会在一个文件上撞车（merge 反费 token），故**主脑亲建**（明日计划写的"可并行 subagent"是选项非强制）。冷审走项目强制的 inference-reviewer（审者≠建者）。
   - **link_prediction**（邻域相似度 + 留出边 AUC，纯 Py networkx+sklearn）：5 预测器（common_neighbors 计数 / Jaccard / Adamic-Adar / resource_allocation / preferential_attachment）在**训练图**上打分；留出 15% 真实边作正样本 + 等量非边作负样本（1:1）；`roc_auc_score(labels, scores)` 评估（1=留出真边、higher→更像边）；top-K 预测新连边=最佳预测器在全图「共享≥1 共同邻居的非边」候选上打分（邻域预测器自然支撑集，候选有上限 cap）。诚实披露：单次随机划分（seed 固定）AUC 会波动、缺失边当未来真边是标准假设、1:1 非真实类别比、top-K 候选限制。
   - **stochastic_block_model**（谱选块数 + SBM 拟合，纯 Py numpy+sklearn）：**关键设计 = 块数 K 由邻接谱选**（`_estimate_n_blocks`：|特征值| 超出随机矩阵体边界 2√⟨k⟩ 的个数，Lei-Rinaldo/Le-Levina 社区数估计），**刻意不用似然/ICL 选 K**——probe 实测非度修正 Bernoulli SBM 的 ICL 会把单个稠密 Erdős–Rényi 块过度切分（K=2 ICL −697.9 vs K=4 −697.7 近乎打平、噪声决定）。`_fit_sbm`：ASE（top-K 特征向量 ×√|λ|）→KMeans 硬分配 + MLE 块连接阵 B[r,s]=边/对 + Bernoulli 对数似然；报同配/异配（块内 vs 块间均值）+ ICL（仅诊断、非选择器）。诚实披露：硬分配非完整变分 EM、二值无向、弱结构落入谱体内→单块。
   - **ergm**（指数随机图，R statnet/ergm MCMC-MLE + 优雅降级）：有 ergm 包→`net ~ edges + gwesp(0.25,fixed=TRUE)` 系数（对数几率）/SE/p/AIC/BIC；**缺包→纯 Py CUG 条件均匀图检验**（观测全局传递性 vs 同 n 同 m 随机图 G(n,m) 零分布、单侧 p=(#null≥obs+1)/(nsim+1)、z）。**本机 R 在但 ergm 包未装→走 CUG 降级路径**（R-MLE 路径冷审静态确认 + 当 ergm 在场时 skipif 测试会跑）。term 白名单（`_ERGM_TERMS` + charset 正则）防 R 公式注入——节点标识符写入 temp CSV 数据、绝不进公式。temp CSV 写输出目录、finally 删。
   - **冷审（inference-reviewer，审者≠建者）ZERO MUST / ZERO SHOULD**：reviewer 用 Bash 经验验证了 AUC 方向/无泄漏、planted-SBM 谱 K + B 矩阵恢复（ARI 1.0、Pin/Pout 精确）、ER 图不过切、CUG p·z（caveman z=32.9 / ER p=0.64）、ergm 注入正则可绕过性、R `network()` 字符 ID + 重复边语义。**2 NICE 已应用**：SBM 热图标题 "ICL-selected"→"spectral-selected"（与谱选设计一致）、yaml SBM method 字段 "(spectral estimation + ICL)"→"(spectral block-count + SBM fit)" + n_blocks/max_blocks 参数文案对齐谱选。
   - **未做 / 待办（NICE，记此）**：① ergm R-MLE 路径在本机未端到端验证（ergm 包未装；CUG 降级已验、R 代码静态审过）——装 statnet/ergm 后跑 `test_r_ergm_path`（现 skipif）确认列序（estimate/SE/p）；② `network_science.py` 现 **1214 行**，逼近 1200 软提升阈值（硬护栏 1500）——**下次往 network_science 加方法前应提升为 `branches/network_science/` 包**（每方法一模块 + 共享 `_resolve_edges`/`_build_graph` 搬入）；③ link_prediction 负样本从全图非边采（非训练图非边），immaterial（留出正样本已排除出负池）；④ ergm CUG 降级在有向图上用无向投影算传递性（传递性本是无向概念，可加一句注）；⑤ SBM 仅二值无向（度修正 DCSBM / 加权变体未拟合）。
   - **教训**：① SBM 块数选择**不能用非度修正似然/ICL**（会过切分度异质块）→改谱方法（probe 当场逮到 ICL 近乎打平，胜过"看着像对"）；② 同一 family 文件的多方法主脑亲建比并行 worktree 省事（并行只在文件不相交时划算）；③ R 桥方法即便本机缺包也要把 R 路径写对 + 降级写对，冷审静态审 R 代码 + 经验验证降级路径。
   - **下一波候选**（用户 next-batch 明日计划剩余）：Bayesian GAM / 状态空间贝叶斯；药理剂量反应 EC50/4PL（用户说方法波最后做）；网页真·视觉重塑·迭代2（卡用户渲染反馈）。

0. ✅ **代码健康审计波（2026-06-26，ruff/vulture/radon + AST 查重 → 修真问题 + 固化 CI 护栏）**：用户:"可以开全代码健康审计"。装 ruff 0.15 / vulture 2.16 / radon 6.0（加入 `dev` extra）。**核心结论:代码本就健康**——自动扫描的"问题"绝大多数是项目的意图约定/框架惯用法,真正可清理的有限。
   - **ruff 校正扫描**(select F+B,**刻意不管样式**):全量 429 → 配置后 81 真问题。**配置识别意图约定**(pyproject `[tool.ruff]`):① F841 unused-local **忽略**——引擎宪法规定每个 handler 统一解包 `df,fp,entry,cfg,d=ctx...` 即便不全用(220 个 F841 里 203 是这约定、仅 17 非约定);② run.py F401 **per-file-ignore**——它是 helper **再导出枢纽**(57 个"未用"导入实为 re-export 给 branches,删了全崩);③ FastAPI `File()/Depends()/Query()/Form()` 默认参数 **extend-immutable-calls**(B008 假阳,是框架惯用法);④ B905 zip-strict **忽略**(加 strict= 改行为非 bug)。**修的真问题**:60 个真未用导入(safe `ruff --fix`,非 run.py)、13 空 f-string、6 未用循环变量(→`_`)、2 B904(app.py 路径校验 `raise HTTPException...from None`,诚实抑制内部 ValueError 链)。全量验证未用导入清理无副作用(registry 280/catalog 280 不变)。
   - **vulture 死代码**:min-confidence 90 → **0**;60 → 12,其中 8 是 FastAPI 路由 handler(框架调用、假阳)、make_timeseries 有用;**4 个真死函数已删**(grep 全仓含 tests/ 确认 0 引用):`trends.snapshot_age_days`、`field_trials._ss`(嵌套、gm 仍被真 SS 用故只删 _ss)、`marketing._kind_of`、`resource_lca._categories`(半建的多影响类别 helper、从未接线)。删后 ruff 复查无新孤儿导入。
   - **AST 归一化查重**(函数体 identifier-normalized 哈希、≥8 语句):全 58k LOC **仅 2 组跨文件克隆**,且 byte 核对后确认**均文档化意图**——`distribution_extra._resolve_column`(docstring "Mirrors distribution.py")与 `_periodogram_period`(state_space/timeseries,docstring "kept local so this family is self-contained",仅 docstring 差)。这是项目**刻意的"family 自包含"**设计(小 helper 宁复制不耦合),**尊重架构、不强拆**(house rule:不撤销文档化的人为决定)。其余跨模块同名 helper(_save_fig×16/_num/_numeric_cols 等)亦同理意图化。
   - **radon 复杂度**:热点(cohens_kappa CC=60、cmh_test 58、life_table 40…)均为正当的分析 handler(单函数内解列+多统计+CSV/图/摘要),平均 C(14.5);**信息性、不重构**(模块≤1500 行护栏已控文件膨胀;盲拆高复杂统计函数风险高且未被要求)。
   - **固化 CI 护栏**(自愈防复发):`tests/test_lint.py` 跑 `ruff check researchforge tests` 断言 0 违规(无 ruff 优雅跳过);ruff 入 `dev` extra。以后未用导入/未定义名/重定义/likely-bug 无法回归。
   - **健康度量**:0 个未定义名(F821)、0 个重定义(F811)、重复极少且全意图化、死代码仅 4 处(已清)。这是**很健康的代码库**。
   - **教训**:① lint 前必辨"意图约定 vs 真问题"(re-export 枢纽 / 统一 ctx 解包 / FastAPI 默认参数 / 自包含复制)——盲 `--fix` 会拆掉 re-export 致全崩;② 查重要 **byte 对比**(归一化哈希抹去字符串常量差异、会把行为不同的函数判为克隆);③ 删死代码前 **grep 全仓含 tests/** 确认 0 引用(vulture 只扫了 researchforge/、tests 用到的会假阳);④ 把高价值检查**固化成 CI 护栏**而非一次性扫描。
   - **下一步(用户已定)**:方法波——贝叶斯纵深(GP/Bayesian survival/DAG)、因果纵深(MR/TMLE/中断时间序列)、生态种群/ERGM;药理最后;网页真·视觉重塑等用户渲染反馈。
0. ✅ **贝叶斯纵深 + 生态种群波（2026-06-27，6 builder subagent 并行 + 主脑集成 + 6 冷审）**：用户"贝叶斯纵深(GP/贝叶斯生存/DAG)、生态种群 多个subagent做"。6 个 builder subagent worktree 并行,**本轮无人撞 session 限额**(各自完成),主脑逐个核完整性、集成、跑测、修、派冷审。引擎 **289 法/45 族**(无新族)。
   - **gaussian_process_regression**(bayesian 族,sklearn GaussianProcessRegressor,ConstantKernel*RBF(ARD)+WhiteKernel,经验贝叶斯):留出 75/25 报 R²/RMSE/**95% 预测区间覆盖率**/lengthscale/log-marginal-lik;X,y 在训练集标准化→预测回传原尺度。**主脑自验**(回传 `mean*y_sd+y_mu`、`std*y_sd`、覆盖率 mean±1.96σ 全对;GP 拟合=已审 sklearn)→不派冷审。2 测绿。
   - **bayesian_survival**(survival 族,PyMC Weibull AFT 右删失):`pm.Censored(upper=where(event,inf,dur))`——事件 upper=+inf 走 logpdf、删失 upper=dur 走 log-survival(冷审逐项确认**未翻转**=正确删失似然);AFT 加速因子 AF=exp(β/sd)>1 表生存更长、HDI;复用 bayesian_mcmc 助手。冷审 **ZERO MUST**(3 NICE:中位生存为后验均值插值无 CI、无 PPC[已指向 KM]、单协变量 HDI squeeze 已覆盖)。4 测绿。
   - **dag_structure_learning**(causal 族,PC 算法):偏相关经子相关阵精度求 ρ=−P[0,1]/√(P00·P11)、Fisher-z(df=n−|S|−3)、PC-stable 骨架、v 结构(k∉sepset)、Meek R1-R3(R4 省略=sound)。冷审 **ZERO MUST**(确认精度公式/df/碰撞规则/空 sepset/R4 省略全对),**应用 1 SHOULD**:骨架分离集只搜 `adj(i)\{j}`→改 `(adj(i)∪adj(j))\{i,j}`(标准 PC 双侧,稠密图防多余边,仍 PC-stable)。**剩 NICE**:冲突碰撞定向后写覆盖(干净模拟不触发)。5 测绿。
   - **capture_recapture**(ecology 族,闭种群):Chapman(K=2,偏差校正+方差)/Schnabel(K≥3,M_t=t 前累计标记)/M0 MLE(定点 n=N(1−(1−p)^K))。**冷审逮 1 MUST(已修)**:M0 渐近方差分母 `(1−p)^−K + Kp/q − K` **错**→正确 `(1−p)^−K − 1 − Kp/q`(冷审从 M0 profile 对数似然的 Fisher 信息**第一性原理推导**+对两份代数等价发表式核验;原式两项错、小 p/大 K 时变负→静默 NaN SE;无测试断言该 SE 故未被捕获)。Schnabel/Chapman/M0 点估计经核对正确。4 测绿。
   - **occupancy_model**(ecology 族,MacKenzie 单季 ψ(.)p(.)):**d=0 站点混合似然 ψ(1−p)^K+(1−ψ)**(占用未检+未占用,冷审确认正确)、logit 尺度 MLE+数值 Hessian+delta SE、NaN 守卫。冷审 **ZERO MUST**(1 NICE:披露用了哪些列作 visit)。**主脑集成修**:converged 原 `bool(res.success)`,BFGS 在平坦占用似然上不可靠(估计对但报未收敛)→改**梯度范数 fallback `success or ‖jac‖<1e-3`**(同 choice.py 先例)。3 测绿。
   - **species_richness**(ecology 族,非参丰富度):Chao1(偏差校正+对数正态 CI)/Chao2/jackknife1-2/ACE(Chao&Lee,rare≤10);**丰度 f1/f2(单/双个体) vs 频率 q1/q2(单/双站点)严格分开**(经典坑已守)、regime 自动(全 0/1→incidence 否则 abundance)。冷审 **ZERO MUST**(逐公式对 Chao&Chiu 2016/EstimateS 核验)。5 测绿。
   - **教训**:① 估计量方差/PC 分离集是真坑——冷审从**第一性原理推导**(Fisher 信息)逮到 M0 方差错,胜过"看着像对";② subagent 自写 `converged=res.success` 在平坦似然不可靠→梯度范数(已第 3 次踩,应固化为惯例);③ 6 subagent 并行本轮全完成(任务边界清晰+文件不相交+spec 给足公式/红线/exemplar);④ 昨天的 ruff CI 护栏继续逮新 test 未用 import pytest。
   - **下一波候选**(用户 next-batch 多选剩余):网络科学 ERGM(指数随机图)/链路预测(生态种群已做);药理剂量反应(EC50/4PL)用户说最后;网页真·视觉重塑等用户渲染反馈;贝叶斯再扩(GP 已做,可 Bayesian GAM/状态空间)。
0. ✅ **因果纵深波：mendelian_randomization + interrupted_time_series + g_computation（2026-06-27，主脑亲建 3 法 + 3 冷审）**：新一天，用户"继续"。接 next-batch 用户多选的**因果纵深**(顺势刚建的 IV/2SLS)。新建 `branches/causal/{mendelian_randomization,interrupted_time_series,g_computation}.py`(family causal)+ `catalog/entries/causal_extra.yaml` + 3 测文件。引擎 **283 法/45 族**。每法先 probe 验统计、再写、再冷审。
   - **mendelian_randomization**(两样本汇总数据 MR,每行=一个遗传工具的 bx/sex/by/sey):IVW(过原点加权回归 Σw·bx·by/Σw·bx²、Cochran Q、**乘性随机效应 SE max(1,√(Q/df))**)/MR-Egger(by~1+bx 加权 LS,斜率=方向性多效性校正后因果效应、截距=多效性形式检验)/加权中位数(Wald 比 by/bx 逆方差加权中位、参数自助 SE)+散点(IVW/Egger 斜率)。**冷审逮 1 MUST(已修)**:MR-Egger **必须将各变异定向为正暴露效应**(bx<0 则翻 bx&by)——IVW/中位数符号不变、但 Egger 斜率/截距非符号不变,真实 GWAS β 混合符号→不定向则 Egger 错;我的恢复测试用全正 bx **掩盖了 bug**→已加**混合符号回归测试**。**应用 2 SHOULD**:① Egger SE 离散 φ 截断≥1(`max(1,Σw·resid²/df)`,与 IVW 一致、与 MendelianRandomization 包默认一致);② 加权中位数自助用**固定原权重 rw=bx²/sey²**(Bowden 2016),不按 bxb 重算。**剩 NICE**:自助对近零 bxb 敏感(中位数较稳、已有弱工具披露)。
   - **interrupted_time_series**(分段回归 ITS):y_t=b0+b1·t+b2·D+b3·(t−T0)·D,**b2=即时水平变化、b3=斜率(趋势)变化**、干预后斜率=b1+b3;**Newey-West HAC SE**(maxlags≈n^0.25)+Durbin-Watson;反事实=干预前趋势外推(图);干预时点 config(时间值或行序号,缺→序列中点占位+诚实标注)。冷审 **ZERO MUST**(系数槽位/HAC 传播/反事实/占位全对)。**剩 NICE**:add_constant 可加 has_constant='add' 防御(门控已保证 t/D/ts 变异、不会触发);可补"单组无对照系列 ITS 最弱"披露;maxlags 可用 Newey-West floor(4(n/100)^(2/9))。
   - **g_computation**(参数 g-公式/标准化):拟合结果模型 E[Y|T,W](含 **T×W 交互**允许异质效应)→对**全样本**预测 do(1)/do(0) 取均差=ATE(在观测协变量分布上标准化);连续 Y→OLS(均差)、二值 Y→logistic(**风险差**);nonparametric bootstrap CI;报 E[Y|do(1)]/E[Y|do(0)]/朴素差对比。**主脑 probe 修**:`add_constant(has_constant='add')`——do(1) 设计 T 列全 1 会与默认 intercept 跳过冲突致形状错。冷审 **ZERO MUST**(标准化/交互一致/二值风险差/bootstrap/披露全对;确认非双稳健、对设定误差敏感、可与 aipw 印证)。**剩 NICE**:正性仅披露未诊断(可加重叠/外推检查)。
   - **教训**:① **MR-Egger 符号定向是真坑**——恢复测试必须含混合符号 β,否则掩盖;② 外部统计法先查参考实现的 SE/权重约定(Egger 离散截断、加权中位数固定权重),别凭感觉;③ 新方法一律先 probe(IVW/Egger/g-formula/ITS 都 probe 过,g-formula probe 当场逮到 add_constant 形状坑);④ 全量跑逮到 lint guard 失败(3 处 test `import pytest` 未用)→昨天固化的 ruff CI 护栏当场生效、清理。
   - **下一波候选**(用户 next-batch 多选剩余):贝叶斯纵深(GP/Bayesian survival/DAG)、生态种群/ERGM(标记重捕/占有率/物种分布/指数随机图);药理剂量反应(EC50/4PL)用户说最后;网页真·视觉重塑(等用户渲染迭代1反馈)。
0. ✅ **有向结构贝叶斯 SEM(中介/通径) + 网页视觉重做·迭代1（2026-06-26，主脑亲建 + 1 冷审 ZERO MUST）**：用户:"网页视觉重做 / 有向结构 SEM 三个都做"（并经 AskUserQuestion 定:SEM 先做、网页盲做一版→渲染迭代、全代码审计作独立下一波、下波方法=贝叶斯纵深+因果纵深+生态种群/ERGM）。
   - **有向结构贝叶斯 SEM**(bayesian_sem 第 3 模式,sem.py):model_spec 加 'F2 ~ F1' 有向回归→**递归结构 SEM**。模型 η=Bη+ζ → Cov(η)=(I-B)⁻¹Σ_ζ(I-B)⁻ᵀ(B 拓扑序严格下三角=递归),边际 Σ_y=ΛCov(η)Λᵀ+Ψ。报**标准化路径系数**(潜变量间有向结构效应=中介/通径,=raw×SD(自变量因子)/SD(因变量因子) 逐 draw)+内生因子 **R²**。**关键 probe 教训(2 轮)**:① 仅 HalfNormal 正锚定对内生因子(自由方差)不够→路径**符号双峰**(4 链 ±0.5 分裂、pooled→0、rhat 1.7);改**标记变量识别(每因子首指标载荷固定常数=1)**定尺度+定符号→STD 路径 [0.63,0.507] vs 真 [0.6,0.5]、rhat 1.002、无双峰。② 含回路(非递归)→`_topo_order` 返 None→诚实退回相关多因子 CFA。冷审 **ZERO MUST**(2M 模拟验 Cov(η) 公式 3dp、(I-B) 单位下三角恒可逆 det=1、标准化、R²、拓扑/解析全对),应用 SHOULD:路径区间原 hardcoded percentile(3,97)=94% ETI 且忽略 hdi_prob 且误名 HDI→改 **az.hdi 真 HDI 读 sc['hdi']**(与单/多因子路径一致)、收敛检查纳 **lam_free**、_branch_bayesian_sem docstring 同步(原说"结构回归待做")。**剩 NICE**:2 指标因子在结构路径下弱识别(每因子都有边时不触发,可加软告警);R²/路径 plug-in 后验均值(与既有一致)。bayesian_sem 现**三模式齐**:单因子 CFA / 相关多因子 CFA(因子相关) / 递归结构 SEM(有向路径)。5 测绿。
   - **网页视觉重做·迭代1**:用户选"我盲做一版较大改动→你渲染后逐条反馈迭代"。**CSS/token 层重做**(选择器/元素 ID/fetch 契约/i18n 全保留→test_web 19 绿、GET 200、契约不动):暖纸色板(--bg #f5f7f3)、卡片圆角 14→16、分层阴影(+--shadow-lg)、accent 渐变(--grad)、深绿品牌字(--accent-deep)、章节标题(h2)加 accent 短杠、推荐卡 hover 上浮(translateY -1px)+更深阴影、严谨度灯点 11→12px、内容列 960→980。DESIGN.md 同步新 token 值。**诚实立场**:主脑无浏览器、看不到渲染→**这是迭代1**,需用户打开 http://127.0.0.1:8000 逐条反馈(太挤/层级弱/某块不好看)再精准迭代(清单 docs/web-user-testing.md)。
   - **教训**:① 贝叶斯潜变量结构模型——内生因子自由方差下"正锚定"不足以定符号,必须**标记变量 marker=1**(经典 SEM 识别);② 复杂模型先 probe(2 轮 probe 才定对识别),省测试反复;③ commit 前查 guard 退出码(本轮已用 `if guards ok then commit` 门控,未重蹈上轮 biases 键覆辙)。
   - **下一步(用户已定)**:**全代码健康审计作独立一波**(装 ruff/vulture/radon + AST 查重,修真问题,固化 CI 护栏);然后**方法波**(贝叶斯纵深 GP/Bayesian survival/DAG、因果纵深 MR/TMLE/中断时间序列、生态种群/ERGM)。药理剂量反应仍排最后。
0. ✅ **iv_regression→真 2SLS + 多因子贝叶斯 SEM + 网页 UI 打磨（2026-06-26，主脑亲建 3 轨 + 3 冷审全 ZERO MUST）**：用户:"网页真·UI 优化(参考 awesome-design-md)、多因子贝叶斯 SEM、iv_regression→真 2SLS 三个都做"。本轮 builder **全主脑亲建**(用户未要 subagent;上轮 subagent 撞 session 限额);3 冷启动 inference-reviewer(审者≠建者)。无新 id(两既有条目升级),引擎仍 **280 法/45 族**。每轨独立 commit。
   - **iv_regression→真 2SLS**(causal 族,清掉审计发现的 2 个"空洞"法之一):原仅 guidance-only;现 config 给 instrument/instruments 时经 **linearmodels IV2SLS** 真跑——内生回归元因果系数(稳健 SE+95% CI)、第一阶段弱工具 F、Wu-Hausman 内生性检验、Sargan 过度识别(过度识别时)、朴素 OLS 对照(差异=内生性偏误纠正)。阶条件守卫(工具≥内生);无工具→诚实指引(无法自动选有效工具=排除约束需领域知识);无 linearmodels→降级。**主脑 probe 确认 IV2SLS API**(first_stage.diagnostics f.stat/wu_hausman/sargan,β 恢复 2.03 vs 真 2.0)。冷审 **ZERO MUST**,应用 2 polish:弱工具(F<10)下稳健 CI 仍不可靠(cov_type='robust' 非弱工具稳健)→荐 Anderson-Rubin/CLR;F 未提取诚实说"无法判断强度"。4 测绿(过度识别恢复/恰好识别/无工具指引/无 linearmodels 降级)。catalog 加 params + executor_ref py::iv_regression。
   - **多因子贝叶斯 SEM**(sem 族,bayesian_sem 扩展):model_spec 定义 ≥2 因子(F1 =~ a+b+c \n F2 =~ d+e+f)→**相关多因子 CFA**(边际化 Σ=ΛΦΛᵀ+Ψ,Φ=因子相关阵)。**关键 probe 教训**:① 初版无锚定→per-factor 符号不定性,链分裂、因子0载荷塌到~0;改**每因子首指标正锚定(HalfNormal marker)**→恢复 lam≈真值、Φ[0,1]=0.468 vs 真 0.5、rhat 1.004。② `pm.LKJCorr(n=k)` 在本版 PyMC **返回 k×k Cholesky 因子(单位范数行)**非旧版扁平向量→Φ=L@L.T。报因子相关(=潜变量标准化结构关联,两因子时=标准化回归系数)+per-factor omega+载荷/HDI/残差/因子相关阵 CSV。单因子路径不变(默认)。冷审 **ZERO MUST**,应用 SHOULD(交叉载荷被静默归首因子丢弃→现 ⚠ 披露)+NICE(_parse_measurement 合并重名因子、剥离 lavaan NA*x/0.7*x 修饰符、**LKJCorr 形状守卫**:非 k×k 时诚实退回不产广播垃圾)。4 测绿(单因子 3 + 多因子恢复 1)。**剩 NICE**:plug-in omega 用后验均值(Jensen ~1%,与单因子一致);有向结构通径(中介/通径)仍待做。
   - **网页 UI 打磨**(CSS-only,保全部 id/契约/i18n):无障碍 prefers-reduced-motion(关动画/过渡)、a/summary focus-visible 焦点环对齐、::selection accent 色、安静滚动条(FF+WebKit)、smooth scroll。**诚实立场**:主脑无浏览器、盲改成熟设计(DESIGN.md 即由现有 UI 提炼)有回退风险→**只做客观安全的附加项**,不做高风险盲重写;真·视觉重塑需用户本地渲染反馈(清单 docs/web-user-testing.md)。test_web 19 绿、GET 200、新 CSS 在位。**移除了一处会成死 CSS 的 body.busy**(无 JS 切换=空洞,自审撤掉)。
   - **教训**:① 护栏链用 `;` 致 guard 失败仍 commit(编辑 biases 误删 `biases:` 键→statistics.yaml schema 失效+config guard 级联)——guard 当场抓到、立即 fix-forward;**commit 前应查 guard 退出码**(别 `cmd; git commit` 盲链)。② 贝叶斯多因子潜变量:per-factor 符号不定性必须 marker 锚定,否则池化均值塌陷。③ 外部库新 API 先 probe(IV2SLS/LKJCorr)再写,免测试反复。
   - **下一波候选**:药理剂量反应(EC50/4PL,用户说最后做)、网页真·UI 视觉重塑(交互式,需用户渲染)、有向结构贝叶斯 SEM(中介/通径)、网页用户实测反馈回收、pls_sem(仍 guidance-only,需测量模型无法自动)。
0. ✅ **系统 GMM + Hausman-Taylor + 贝叶斯 SEM 解锁 + 网页设计/实测包（2026-06-26，subagent 撞 session 限额→主脑亲建）**：用户:"推掉当前(已推 3ab1fec);subagent 做 系统 GMM/Hausman-Taylor、贝叶斯 SEM、网页用户实测;网页优化参考 awesome-design-md(下一波可能)"。**2 个 builder subagent 撞账户级 session 限额**(B 0 tool-use、A 9 tool-use 即停,worktree 已清无可救产出)——按"不重试入限额"主脑全部**亲建**。引擎 **280 法/45 族**(+system_gmm/hausman_taylor 2 法;bayesian_sem 由"降级提示"转"可运行")。每轨独立 commit 防额度中断丢进度。
   - **panel_iv.py**(新,family econometrics,R/plm 委托+优雅降级):**system_gmm**(Blundell-Bond 系统 GMM,`pgmm(..., model="twosteps", transformation="ld")`;镜像 dynamic_panel_gmm:滞后被解释变量 lag≥2、GMM 工具窗 lo:hi 默认 2:4 抑过度增殖、Sargan+AR(1)/AR(2);水平+差分矩条件,高持续序列优于差分 GMM、代价是初始条件/均值平稳假定)。**hausman_taylor**(`plm(model="ht", inst.method="baltagi")`;**自动检测时不变回归元**=组内 std≈0、用外生时变回归元的组内均值作工具识别时不变系数、config endogenous 标内生;无时不变变量→诚实跳 RE/FE)。列名标识符守卫、temp CSV finally 清理、**honor config unit/time**(修测试逮到的:profiler 未把 person/wave 判为面板→handler 须认 config 覆盖)。5 测绿(R 实跑恢复 system-GMM 持续性∈(0.1,0.9)、HT 时不变 z≈1.5±0.6;R 缺失降级;无时不变跳过)。
   - **bayesian_sem 解锁**(sem.py 重写 _branch_bayesian_sem):原仅诚实降级(需 R blavaan+JAGS/Stan 编译器)→**现代 PyMC 单因子贝叶斯 CFA**(测量模型)。**关键修**:初版用逐观测潜因子分数 eta(n 维)+`eta*lam` 乘性 funnel→NUTS 混合差、载荷塌到 0(低 ESS);改**边际化潜因子** 指标~`MvNormal(0, Σ=λλᵀ+Ψ)`(因子方差固定=1 识别尺度)→仅 2p 参数、条件好、适度抽样稳定恢复载荷。报标准化载荷+94% HDI、残差 SD、**McDonald's ω 构念信度**+AVE、R-hat/ESS;符号不定性→载荷统一翻正(约定,已披露);复用 bayesian_mcmc 的 _have_pymc/_sampler_cfg/_convergence/_conv_note/_hdi_bounds/_forest;无 pymc 诚实降级。catalog 条目改 method/描述/produces/biases/params;test 重写 3 测绿(载荷恢复 within 0.25+降级+不足跳过,38s)。⚠ 单因子测量模型≠含潜变量间结构路径的完整 SEM(后续扩展)。
   - **✅ A/B 冷审已补(额度恢复后补派,两份冷启动 inference-reviewer,审者≠建者)——均 ZERO MUST-FIX**:① **panel_iv(system_gmm/hausman_taylor)**:R/plm 调用正确、**HT 工具集成员关系无符号错**(instruments=regressors−endogenous=外生集,确认)、镜像 dynamic_panel_gmm 先例;**应用 2 SHOULD**:system_gmm 补 lo<2 flooring 警示(与差分GMM 披露对齐)+ HT 加**阶条件守卫**(外生时变工具数 < 内生时不变数→欠识别,诚实跳转 RE/FE,避免 plm 原始报错);NICE Sargan-vs-Hansen 标注已加。② **bayesian_sem CFA**:**经验验证** Σ=λλᵀ+Ψ(n=2M 模拟 |经验−解析|≈0.0012)、尺度识别 PD、**符号翻转含 HDI 界交换正确**(避开了 naive −lo,−hi 经典 bug)、omega/AVE 公式对;**应用 1 SHOULD**:载荷符号双峰会虚高 lam 的 R-hat→**按链符号对齐后再算收敛**(去伪非收敛警告,不改估计)。**剩 NICE**(未改,记此):omega 用 (E[ψ])² 而非 E[ψ²](Jensen,~1% 可忽略);贝叶斯 CFA 可 in-model 锚定符号。**所有 SHOULD-FIX 已应用、8 测仍绿。**
   - **网页(awesome-design-md 参考 + 用户实测)**:**`web/DESIGN.md`**——DESIGN.md 风格设计系统规格(锚定现有 index.html `:root` tokens:森绿学术色 --accent #2f6f4f/系统字含 CJK 回退/14px 圆角/玻璃 sticky header/zh-EN pill 切换/严谨度灯保留绿黄红/焦点环/无障碍守卫/Agent 提示指南);把用户给的参考落成可复用工件。**`docs/web-user-testing.md`**——真人实测清单(**主脑无浏览器、做不了真·人测**→结构化交用户跑:核心流/config 覆盖/中英切换/边角/响应式/无障碍/性能,附"如何回报")。自动审计:test_web 19 绿、GET / 200(48683 字节)、29 个 data-i18n 键、tokens 在位。**刻意未做高风险 index.html 重写**(保全部元素 ID + fetch 契约稳定;真·UI 优化=下一波交互式做,用户已暗示"下一波可能")。
   - **教训**:① subagent 撞账户 session 限额=0 产出,主脑亲建比反复重试入限额更稳(house rule:别重试入限额);worktree 撞限额后被清,无可救产出→主脑从零建。② R/plm 委托类方法可"实测恢复验证 + 自验公式"为主,冷审为外部金标准上的接线确认(额度恢复补);③ 贝叶斯潜变量模型避逐观测潜分数乘性 funnel→边际化(Σ=λλᵀ+Ψ)条件好得多。
   - **下一波候选**:药理剂量反应(EC50/4PL,用户说最后做)、**网页真·UI 优化**(按 web/DESIGN.md 交互式重塑 index.html,用户暗示下波)、多因子贝叶斯 SEM(结构路径)、网页用户实测反馈回收、**iv_regression 改为 config instrument 提供时真跑 2SLS**(现 guidance-only;审计发现的 2 个"空洞"法之一,另一 pls_sem 需测量模型无法自动)、guidance_only catalog 标志(iv_regression/pls_sem)。
   - **✅ 代码审计(2026-06-26,用户要"完整 review 是否有问题/冗余/不完整空洞")**:结构完整性**280 id↔280 handler 完美 1:1**(无破损 py:: ref、无孤儿 handler)。**空洞法仅 2 个**:iv_regression / pls_sem——均**有意 guidance-only**(需用户给工具变量/测量模型,自动选会出错=诚实设计,非 stub),已记增强候选。**冗余**:逮到 panel_iv 自定义 `_gmm_lags` 与 core.py 既有版本重复→改为复用 canonical(DRY);其余跨模块同名 helper(`_save_fig`/`_num`/`_numeric_cols` 等)为**有意的 per-family 自包含小工具**(符合"一模块一族"架构,合并会引入耦合,不动)。**新法两份冷审 ZERO MUST**(见上)。全量绿。
0. ✅ **计量经济补全(限因变量族) + 贝叶斯随机斜率/PPC + 报告选键智能 + NICE backlog 清理（2026-06-26，2 builder subagent + 主脑;2 冷审全 ZERO MUST）**：用户:"计量经济补全/贝叶斯随机斜率/报告选键更智能/各 NICE backlog 开 subagent 做,药理最后再说,先做这些"。引擎 **278 法/45 族**(+4 法,均落在既有族 econometrics/bayesian——无新族)。2 个 builder subagent 这次**都没撞限额**(各自完成);主脑集成+跑测+派 2 冷审。
   - **limited_dependent.py**(新文件,family econometrics):**tobit_regression**(Type-I 删失正态 MLE,未删失 logφ((y−Xβ)/σ)/σ + 删失 logΦ;logσ 参数化+数值 Hessian SE;**β=潜变量效应≠对 E[y] 边际**[报 Φ(x̄β/σ) 缩放因子];删失质量≥5%&≥3 才跑、否则诚实跳 ols)、**truncated_regression**(截断正态 MLE,LL 除以截断概率 −log(1−Φ((a−Xβ)/σ));对比朴素 OLS 衰减)、**heckman_selection**(Heckman 1979 两步:probit 选择方程→逆 Mills 比 λ=φ/Φ→第二步 OLS 加 λ;λ 系数=ρ·σ_ε、其 t 检验=**选择偏差检验** H0:ρ=0;诚实披露**两步 SE 未做 Greene 校正**=朴素 OLS SE、识别依赖排除约束)。冷审 **ZERO MUST**(似然符号/logσ delta SE/IMR 全核对 Wooldridge ch.17·Greene,7 测含恢复+朴素OLS偏差对比)。**应用 2 SHOULD**:① Heckman 第二步行对齐 positional-vs-label 脆弱(λ 位置赋值 vs y/predictor 标签 reindex,仅当非 RangeIndex 才暴露,当前 read_csv 恒 RangeIndex 故潜伏)→handler 顶 `df=df.reset_index(drop=True)` 硬化;② δ_i=λ_i(λ_i+zγ_i) 原对全选中行平均、与第二步 dropna 后样本不一致→carry `_zb`/`_imr` 过 sel_block、δ 用同样本。**应用 1 NICE**:Tobit 也把 naive_ols 写进 estimates(与 truncated 对称)。
   - **bayesian_random_slopes**(扩 bayesian_mcmc.py 850→1082 行<1500,family bayesian):变截距+变斜率多层模型,**LKJCholeskyCov(η=2)相关随机效应 + 非中心化 ab=chol@z**(z~N(0,1) shape(2,n_groups));报总体斜率(标准化+raw 回传 μ_b/sd_x)+HDI、组间斜率 SD、**截距-斜率相关**(chol_corr[0,1])、ICC、收敛。无组内预测变量→诚实跳指向 bayesian_hierarchical。冷审 **ZERO MUST**(LKJ 非中心化协方差 chol@chol.T、chol_stds/chol_corr 索引[运行实测确认 var 名]、PPC 轴、收敛 np.nanmax 不掩盖真不收敛全核对),**应用 1 SHOULD**(诚实):ICC=**截距 ICC、于 x 均值处**——x 已中心化、随机斜率下总组间方差随 x 变(var_a+2x·cov+x²var_b),单一 ICC 为近似(summary+yaml biases 已加)+ NICE(population_intercept 为 at-mean-x 非 x=0 已披露)。
   - **PPC 后验预测检查**(`_ppc` helper,清 bayesian 族长期 backlog「做后验预测检查」):`pm.sample_posterior_predictive`→**贝叶斯 p 值** P(T(y_rep)≥T(y_obs)) for mean/SD(≈0.5=数据与模型一致、近 0/1=misfit)+az.plot_ppc,全 try/except best-effort、**纯附加**接入 bayesian_random_slopes + bayesian_regression(不改后者既有 estimates/计算)。
   - **报告选键更智能**(`_fmt_estimates` in report_narrative.py,纯展示无推断审):salience 分层(tier0 点估计 > tier1 p 值 > tier2 CI/SE 界 > tier3 记账/诊断),把 `n_*`/`seed`/`rhat`/`ess`/`df` 降到末位、点估计**折叠其匹配 CI 界**为一槽(`coef=1.2 [0.4,2]`)而非各占一槽,保留同层插入序。10 测绿(加 salience 专测)。
   - **NICE backlog 清理(本轮穿插,均文件不相交)**:**actuarial**(q_x/L_x 注释补 ·n 因子[代码一直对、ax_years=a_x·n]、Mack tail-rule 注释 off-by-one[改用 finite_idx 表述]、重尾 Pareto(shape≤1)TVaR=∞ 加显式"数学未定义"提示)、**game_theory**(非零和 game_value 仅 P1 支付→补 `game_value_p2`)、**CEA**(效果值≤0 告警:ACER/ICER 在该象限难解释)、**input_output**(total_output 路径加 A≥0/流量非负守卫,ρ<1 guard 不抓符号)、**hydrology**(FDC 负流量显式守卫[物理径流不应为负=选错列]+n<10 caveat)、**choice**(conditional_logit 的 chosen 列自动检测**名优先**[chosen/choice/selected/...]再回退首个 0/1 列)。
   - **教训**:① 两 subagent 这次都没撞限额(任务边界清晰+文件不相交+spec 给足惯例/红线),主脑仍逐文件核完整性+跑测+派冷审(workflow 不变);② 冷审抓到的"潜伏 bug"(Heckman positional-vs-label,仅非 RangeIndex 暴露)值得修——`df.reset_index(drop=True)` 一行硬化未来 web/clean 路径;③ 报告选键智能=纯展示层,salience 分层把诊断/记账键降权,关键数值更干净。
   - **下一波候选**:药理剂量反应(EC50/4PL,用户说最后再做)、系统 GMM/Hausman-Taylor(R/linearmodels)、贝叶斯 SEM、网页用户实测、各 NICE backlog 剩项。
0. ✅ **运筹+博弈+市场+精算 4 领域波 + 因子库刷新到最新（2026-06-26，4 subagent + 主脑补完/修 + WebSearch 刷因子）**：用户:"运筹/博弈/市场/精算多 subagent + 因子取尽可能新"。引擎 **274 法/45 族**(+11法 +4族)。3 个 subagent 撞会话限额停在中途→主脑核完整性补完。
   - **operations_research**:eoq_inventory/queue_mmc(Erlang-C)/newsvendor。确定性,16 测验证。
   - **game_theory**:normal_form_game(纯NE最优响应+2x2混合NE+IESDS+零和)/shapley_value(精确枚举)。**主脑修真 bug**:位掩码联盟列被鲁棒读取转 int 丢前导零→`zfill` 按最大宽度恢复。冷审 **ZERO MUST**(NE 方向/2x2混合公式/Shapley 全验对 Osborne-Rubinstein/Shapley1953),**已加 SHOULD 披露**(占优/IESDS 仅检纯策略占优、不检混合占优→幸存集可能偏大)。**剩 NICE**:非零和 game_value 只取 P1 支付(标签;summary 已报双方)。
   - **marketing**:rfm_segmentation/customer_lifetime_value/market_basket(手搓 Apriori support/conf/lift)。确定性,测试验证。
   - **actuarial**:life_table(q_x/l_x/L_x/T_x/e_x)/chain_ladder(volume-weighted f_j+IBNR+**Mack SE**)/loss_distribution(MLE+AIC+VaR/TVaR)。冷审 **ZERO MUST**——Mack SE 对 Mack1993 Taylor-Ashe 三角验(CV 0.125)、loss-dist VaR_95 对闭式 lognormal、TVaR 对闭式 CVaR。**剩 NICE**:docstring/catalog 的 q_x/L_x 写法少 `·n` 因子(n=1 时一致,n≠1 不准,代码对);Mack 注释 tail-rule 索引 off-by-one(代码对);重尾 Pareto(shape≤1)TVaR=inf 可加"未定义"提示。
   - **主脑修 subagent 测试**:多处 1e-9 容差 vs 6dp 舍入→放宽 1e-5;actuarial 测试 hand-calc 期望值偏(l_2 74026.72→74025.97、L_2、T_0)→改正确值(实现对,我独立手算+读实现验);actuarial.yaml biases 含 `: `→引号;game 1/3 等舍入容差。
   - **因子库刷新到最新(WebSearch curation)**:LCA `LIBRARY_VERSION` 0.1→**0.2 (as of 2026-06)**;燃料 DEFRA/EPA 2023→**DEFRA/DESNZ UK GHG CF 2025**(diesel 2.57082/petrol 2.06916/天然气 0.18296 kgCO2e/kWh,AR5 GWP CO2e);电网 0.48→**0.458**(Ember Global Electricity Review 2026,2025 世界均值;2024=0.471);GWP100 保持 IPCC AR6 2021(现最新评估);水足迹保持 WFN 真值;water 移出 ILLUSTRATIVE(现仅 energy 占位)。DISCLAIMER 加"无法运行时联网取最新,库带版本号请定期对照官方源刷新"。emergy 转换率库基线 Odum1996 9.26E24(无年度更新,已充分披露 Brown 2010 替代基线)。
   - **答用户"要最新"**:诚实——知识截止+WebSearch curation 取到最新发布(DEFRA 2025/Ember 2026),但**分析运行时不联网**(红线);库带版本+年份,可定期手动刷新。
0. ✅ **水文+选择+能值波 + LCA 因子库(真水足迹) + 报告接数字（2026-06-26，4 subagent 撞限额→主脑兜底补完 + 主脑亲建 emergy/报告/水足迹）**：用户问"水足迹/能值是否公开可存、要最新"。4 subagent **撞会话限额停在不同阶段**——主脑逐个核 worktree 完整性、补缺件、修截断 bug。引擎 **263 法/41 族**。
   - **hydrology 族**(新,subagent 建 handler、主脑补 yaml+test):mann_kendall_trend(MK S+tie-corrected Var+连续校正 Z+Sen 斜率+Gilbert 秩 CI)/flow_duration_curve(Weibull 超越概率)/idf_curve(Talbot 拟合)。冷审 **ZERO MUST**(全公式核对 Hipel-McLeod/Gilbert)。**SHOULD（已记，未改）**:Sen 斜率用秩步长、忽略真实时间不等间距(已披露,等间距序列无影响;修法=time 列解析为数值/日期作分母)。**NICE**:CI 用舍入非 Gilbert 插值;n<10 正态近似 caveat;FDC 负流量守卫。主脑加了带噪 CI 测试(线性测试退化)。
   - **choice 族**(新,subagent 建 handler+yaml、主脑补 test):mnl_choice(MNLogit RRR/伪R²/LR)/conditional_logit(手搓 clogit MLE)。冷审 **ZERO MUST**——**clogit 与 statsmodels ConditionalLogit 逐位一致**(params/SE/llf、梯度 vs FD 3.6e-7、Hessian 7.8e-9)、WTP 对。**主脑修真 bug**:BFGS `res.success` 平坦似然误报未收敛→改梯度范数判据。**已应用 SHOULD**:近奇异 Hessian(cond>1e10)加⚠;n_alternatives 截断均值→报真实(round float)。**剩 NICE**:1e-4 梯度阈绝对(极大量纲属性可能 false 未收敛、但 fail-safe);弹性是代表性近似(已披露);auto chosen 列可加名优先。
   - **emergy_analysis**(主脑亲建,resource 族,确定性核算):Odum U=R+N+F、EYR/ELR/ESI/%renew、转换率库(Odum/Brown/NEAD,基线 9.26E24 披露)。
   - **LCA 因子库 + 真水足迹**:**主脑修 4 处截断 bug**(_resolve_footprint 3/4 元返回混用→统一 4 元、note 用 meta['factor']→.get('value')、缺 category/categories param、n_library_matched 未发→补)。**真·WFN 水足迹**替换占位(Mekonnen&Hoekstra 全球均值:beef 15400/coffee 18900/rice 2500/wheat 1830... L/kg=m³/吨,带 source+year)。water 移出 ILLUSTRATIVE_CATEGORIES(现 energy 仍占位)。
   - **报告接 estimates 真数字**(主脑):`_report(...,estimates)` + `build_narrative(...,estimates)` 加「关键数值」段(前 6 个非 NaN 估计、4g、`_report` 防御包裹)。run.py 调用点传 estimates。9 测。
   - **贝叶斯扩展补审**(上会话进程退出红线缺口,本会话重派):**ZERO MUST**(NB Var=mu+mu²/alpha 验、nu~Gamma(2,0.1) P(nu<2)=1.75% 可忽略、PSIS-LOO compute_log_likelihood 对)。**剩 NICE**:NB 离散用边际 var/mean 非条件(可注、NB 嵌套 Poisson 过判自回退);az.compare 列注过时;Pareto-k 可升为实时标。
   - **答用户**:水足迹/能值/排放因子**多为公开科学常数**(IPCC/EPA/DEFRA/IEA/WFN/Odum),已内嵌带出处+年份的精选库;**"最新"无法运行时联网取**(分析不联网红线),库带版本号可定期刷新。**网页真人实测**主脑做不了(无浏览器),只能自动审计(GET 200/test_web/i18n)——需用户本地实测。
   - **教训**:subagent 撞限额=部分产出,主脑必**逐文件核完整性**(test -f)再集成、补缺、修截断半成品(本轮逮到 _resolve_footprint 多返回值不一致等);worktree 从 origin 分叉、未推的本地改动 subagent 看不到→补缺件自己写而非再派。
0. ✅ **收尾+平台轮：LCA/LCC + Sankey + 报告智能 + 网页打磨（2026-06-25，3 subagent + 主脑 Sankey）**：用户"继续 subagent"做 nexus 可选收尾(LCA/LCC/Sankey)+ 报告智能 + 网页打磨。文件不相交并行。引擎 **257 法/39 族**。
   - **G·life-cycle pair**:`footprint_analysis`(resource:活动量×因子→碳/水足迹+热点+单位强度,**因子用户给、无则降级**)+ `life_cycle_cost`(techno_economic:LCC/EAC/CRF/salvage,r=0 守卫)。确定性,已知值测试 hand-verify(LCC=1248.685/salvage/r=0)→不派 inference 审(宪法:确定性实测验证,同 MFA/techno_economic)。主脑修 footprint share 8dp 舍入 vs 1e-9 容差→放宽 1e-6。
   - **H·报告智能**:`_helpers/report_narrative.py` `build_narrative()` 给报告加「## 解读(自动生成)」(一句话结论+如何解读+关键看点[⚠ 局限单列]+建议下一步[family→方法映射]);`_report` 加 **try/except 包裹的附加钩子**(diff 确认只加 11 行、纯附加、防御性、不改分析)。8 测绿。展示层,无 inference 审。
   - **I·网页打磨**:i18n 完整性审计——上一波已很全,仅修 `renderIssues` 一处裸 `'—'` 走 t();+无障碍(aria-pressed/type=button/bilingual aria-label)+长名/标识换行响应式(overflow-wrap)。**保全部元素 ID + fetch 契约**。GET / 200(48683 字节)、test_web 19 绿、script 平衡、`</html>`。反引号 147=HEAD(奇数预存、文本里有反引号字符、非本次、页面一直正常)。
   - **主脑·Sankey**:给 `material_flow_analysis` 加 matplotlib.sankey 流图(投入→系统→输出+净存量,Σflows 恒=0 满足 Sankey,best-effort try/except);验证 `mfa_sankey.png` 真生成、MFA 6 测仍绿。
   - **教训**:网页 worktree 经 harness 自动合并入主仓(本轮 I 同上轮),主脑跑 GET /+test_web 验;core.py 等引擎核心被 subagent 改→主脑 **diff 确认只动目标函数**再复制;确定性算术方法(footprint/LCC/MFA/Sankey)按宪法测试验证、不耗 inference 审额度。**剩 NICE**:footprint 多影响类别一次跑(现单类);LCC 不平衡/缺 opex 检测边角;报告叙事可接 estimates 数字(现 summary 驱动)。
0. ✅ **nexus 收尾·成本↔物理桥（2026-06-25，2 并行 subagent + 主脑集成 + 冷审）**：用户问 techno_economic 与 resource 怎么互补→答 techno=财务/resource=物理两镜头,缺"桥"。补 3 法(全 resource 族,分文件避冲突),把 $ 与物理单位放同一线,EWF nexus 工具箱打通(财务×物理×权衡)。
   - **E·resource_econ.py**:`cost_effectiveness_analysis`(CEA/ICER:ACER、强劣汰、**扩展劣汰迭代消除**、NMB/wtp 最优、效果=0 剔除)+ `marginal_abatement_cost`(MACC:MAC=成本/减排、升序+累积曲线、no-regret、price 阈下减排)。CEA 前沿逻辑派**冷审**(去劣/扩展劣汰/ICER 索引易错)。
   - **F·resource_mfa.py**:`material_flow_analysis`(质量平衡 投入=产出+NAS、throughput/回收率/资源效率、名称启发式分类可 config 覆盖、NaN 当 0)。纯核算→测试验证(无 inference 审,subagent 正确判断)。
   - 引擎 **255 法/39 族**(resource 族现 5 法:composite_index/input_output_analysis/cost_effectiveness_analysis/marginal_abatement_cost/material_flow_analysis)。E 测 11、F 测多,+守卫全绿。**CEA/MACC 冷审:零 MUST/零 SHOULD**——扩展劣汰迭代算法+索引算术+NMB 规则+MACC 累积/阈值逐行对凸包定义/Drummond 核验全对。**剩 NICE**:① CEA 精确重复行(同成本同效果)→de=0 段 ICER=inf,若排在有限 ICER 选项前可能误判后者扩展劣汰(仅真重复行触发,可折叠重复点);② 负效果值静默接受(ACER/ICER 难解释,可告警);③ abatement_below_price 含已"免费"的 no-regret 量(语义注)。
   - **techno_economic 互补结论(记一笔)**:不用重做;它是财务镜头,resource 是物理镜头,CEA/MACC/MFA 是桥。**剩可选**:LCA 碳/水足迹(resource/单位)、LCC 全生命周期成本(extend techno)、Sankey 流图(MFA 可视化增强)。
0. ✅ **4-track 大轮：MICE + 综合指数 + 投入产出 + 网页中英化（2026-06-25，4 并行 subagent + 主脑集成/修/冷审）**：用户要"分四五个 subagent 干"(MICE+nexus+网页优化+中英)。**文件不相交**并行,主脑逐个集成。引擎 **252 法/39 族**(+missing_data、resource 两族)。
   - **A·missing_data 族**(MICE 你问的):`mice_imputation`(链式方程 M 个完整集→**Rubin 规则**合并,statsmodels MICEData 优先/sklearn IterativeImputer sample_posterior=True 回退)+ `missingness_diagnosis`(缺失模式+MCAR 关联筛查非 Little)。冷审 **ZERO MUST**(Rubin hand-calc 逐项验对、fallback 用 sample_posterior 保证 B>0)。**SHOULD 已修**:MCAR 的 logistic 近完全分离不收敛→χ² 虚高→加 `mle_retvals.converged` 守卫跳过+抑警告+披露跳过数。**剩 NICE**:FMI 用大样本 λ(可改 df 校正);合并 LR 独立性假设(偏宽松,已披露)。
   - **B·composite_index**(resource 族,EWF nexus 指数):OECD/JRC 综合指数(归一+赋权+聚合)。冷审 **ZERO MUST**(熵权 k=1/ln n/几何聚合/cost 反向全验)。**SHOULD 已修**:熵权在 min-max 矩阵上算→压缩权重差异(vs 原始数据熵权)已披露;precondition min_continuous→min_numeric_cols(不误压 count/id 指标)。**剩 NICE**:几何全局 rescale 跨列耦合(zscore 下);eps=1e-6 floor 对近零单位排名敏感(已披露)。
   - **C·input_output_analysis**(resource 族,Leontief):A=Z/x̂、L=(I−A)⁻¹、输出乘数/前后向关联、ρ(A)<1 productivity 守卫。**主脑修真退化 bug**:subagent 的"raw flow 按列和归一"默认→A 列随机、ρ=1、(I−A) 奇异、永不 productive→改**诚实降级请求 total_output**(+改测试断言降级)。冷审 **ZERO MUST** 且**确认修正对**(列随机 ρ=1 不可避)。**SHOULD 已修**:陈旧 YAML 描述旧列和行为(27-28/44/58 行)→对齐降级 + 加 Ghosh 前向关联区分披露。**剩 NICE**:total_output 路径未查 A≥0;looks_coef 检测歧义(已披露)。
   - **D·网页中英 + 优化**:中文/EN 切换(data-i18n + localStorage `rf_lang` + `t()` 路由动态串 + 切换不丢状态)+ 视觉打磨(sticky header/真 spinner/卡片 hover/响应式)。**保留全部元素 ID + fetch 契约**。验证:TestClient GET / 200(47861 字节)、i18n 函数齐全、script 标签平衡、test_web 绿。展示层,无 inference 审。
   - **教训**:① subagent 沙箱禁 py/git,主脑必自验逮到 IO 退化 bug(再验价值);② 多 subagent 同族用**分文件 + 分 yaml**(resource.py/resource.yaml ⊥ resource_io.py/resource_io.yaml)避免冲突;③ 网页 worktree 改动经 harness 合并入主仓,主脑跑 GET / + test_web 验证。
0. ✅ **贝叶斯扩展·计数/稳健/模型比较（2026-06-25，主脑亲建，续贝叶斯重评）**：用户选「贝叶斯扩展」(并问 MICE/nexus——见下方答疑记录)。加到 `bayesian_mcmc.py`(847 行)：
   - **bayesian_poisson_regression**:计数 GLM log 链接,**自动按过离散选 Poisson/负二项**(var/mean>1.5,config family),率比 RR=exp(β_std/sd_x)+HDI。**接上诊断闭环**——slice-2 诊断检测到过离散计数,正是此法用武之地。NB 用 `pm.NegativeBinomial(mu,alpha)`,alpha~Exponential(1)(冷审看是否合理)。
   - **bayesian_robust_regression**:Student-t 似然,nu~Gamma(2,0.1) 估自由度(小 ν=重尾=离群,自动降权,无需手剔),系数同标准化回传。接上 outlier 诊断。
   - **bayesian_model_comparison**:拟合 null/线性/稳健三候选→各 `pm.compute_log_likelihood`→`az.compare` PSIS-LOO,报 ELPD/权重/排名/最优。
   - 测试 7 个(原 4 + 新 3:NB 过离散 RR、robust 抗离群+小 ν、LOO 排名 linear>null)34s 绿。**冷审 ZERO MUST-FIX**(NB mu=exp/RR 回传、Student-t ν~Gamma(2,0.1)=Juárez-Steel 标准/无需下界、LOO 跨族比较 y 同分布有效、compute_log_likelihood 在 with 上下文内正确、az.compare best=rank0/列名 elpd/weight 确认)。**应用 1 SHOULD**:NB 离散先验 `Exponential(1)`(偏向过离散、惩罚 Poisson 极限、几何差)→改 `Gamma(2,0.1)`(均值 20、不惩罚 Poisson 极限)。**剩 NICE**:ν 可设下界>1(非必需);LOO Pareto-k 诊断未 surface(已披露);模型比较候选集固定(未搜预测变量子集);全不同整数计数列会被 profiler 判 id(已知 id-trap)。
   - **用户问答（记一笔，皆未做、为未来波）**:① **MICE 多重插补**=缺失数据金标准(链式方程→M 个完整集→Rubin 规则合并,正确反映插补不确定性);引擎只有简单清洗、**无 MICE**,适合 `missing_data` 族(statsmodels.MICE/sklearn IterativeImputer)。② **能源-水-食物 EWF nexus**=应用域非单一方法;**已有积木**:DEA/SFA(效率)、MCDA 全家(entropy/vikor/promethee/ahp/topsis,权衡)、网络(耦合)、VAR/Granger/协整(动态耦合);**缺**:composite_index(综合指数)、input_output/Leontief(投入产出足迹)——未来可做 resource/nexus 领域波。
0. ✅ **贝叶斯重评·全 MCMC 回归族（2026-06-25，主脑亲建，旗舰深度,解锁原"卡 Stan/JAGS"）**：用户选「贝叶斯重评(需装 PyMC)」。
   - **门禁先验证**：装 PyMC 6.0.1 + ArviZ 1.2.0，**实测 Windows 无 g++ 也能 NUTS 秒级采样**(β=2.92→真3.0、R-hat=1.00、HDI 准)——历史"卡 Stan/JAGS"(需编译器)对现代 PyMC **已过时**。
   - **新族文件 `branches/bayesian_mcmc.py`**(family 仍 bayesian,与共轭 `bayesian.py` 同族,auto-discovery 注册,各文件聚焦)：`bayesian_regression`(弱信息先验+标准化采样→β 回传原尺度+94% HDI+贝叶斯 R²)、`bayesian_logistic_regression`(Gelman N(0,2.5)+OR exp(β_std/sd_x)+HDI)、`bayesian_hierarchical`(变截距**非中心化**部分汇集+ICC+组内可选斜率——贝叶斯杀手锏)。全部 R-hat/ESS 收敛诚实(max R-hat>1.05 标 ⚠)、seed 固定、ArviZ forest 图、**可选+优雅降级**(无 pymc→诚实跳过指向 ols/logistic/glmm,镜像 R 桥)。
   - **主脑修**：ArviZ 1.2.0 `az.hdi` kwarg 由 `hdi_prob`→`prob`(try 两版兼容);去掉触发 FutureWarning 的 `idata_kwargs={log_likelihood}`。4 测(回归恢复 β±0.5/HDI 排零、logistic OR 方向、分层 ICC+部分汇集收缩、无-pymc 降级)8.7s 全绿。conftest SLOW + pyproject `full` extras 加 pymc/arviz。
   - **冷审 1 MUST-FIX（主脑亲验后修）**：ArviZ 1.2.0 默认 `round_to="auto"` 把 `summary()` 的 `r_hat` 格式化成**字符串**("1.00")、ess 成 Int64——`summ["r_hat"].max()` 退化为**字典序比较**,R-hat≥10 时 "10.00"<"9.99" 会**漏判不收敛**(正是 ⚠ 该抓的);亲跑确认 dtype=object→改 `round_to="none"` 取 float + np.nanmax/nanmin(同解 Int64/NA 崩)。**应用 4 SHOULD/NICE**:① logistic 措辞"后验中位"→"OR=exp(后验均值 log-odds)"(实算均值非中位);② 去掉"Gelman 默认"过度声称(Gelman 实为 Cauchy(0,2.5)/0.5-SD,这里 Normal/SD1)→改"弱信息 Normal(0,2.5) 标准化 log-odds 尺度";③ 链数<4 告警;④ 贝叶斯 R² 注释标"近似(后验均值拟合)"。**冷审确认 CORRECT**:back-transform β_raw=β_std/sd_x + HDI 同除(正常数缩放不变性)、非中心化、ICC=σ_a²/(σ_a²+σ²)、先验合理、降级/守卫。测试加 `hdi_low≤hdi_high` + max_rhat 数值型断言防回归。**剩 NICE**：贝叶斯 R²/ICC 可逐 draw 算后验(现点估计);截距未回传原尺度(只报系数/OR);e2e 现真跑采样(默认 1000/1000)略慢(已 slow 标)。
   - **教训**：旗舰深度方法**先验证门禁**(装包+最小采样)再建全族,避免白做;新依赖装后留意是否改变 e2e/既有测试行为(贝叶斯入 top-6 会真采样)。
0. ✅ **聪明选模更深(诊断感知排序) + 可靠性工程领域波（2026-06-25，主脑亲建 A + subagent B + 冷审）**：用户选「领域波 + 聪明选模更深」并行。
   - **A 聪明选模更深·诊断感知排序**（`recommender/recommend.py` 主脑亲建）：把 slice-2 诊断计划从"仅 advisory 文字"升级为**真正影响排序**——`apply_diagnostic_ranking` 给 fired 诊断 prefer 的方法 +12·命中、over 的 −12，**严谨度灯序仍是主键**(只在同灯层内重排,preferred yellow 不越 green)。`recommend()` 保持纯净;重排在 `select_top(plan=,diagnostic_aware=True)` + web service;CLI/web 显示 `📋 诊断契合↑/不利↓`。实测过离散数据 negbin↑/poisson↓。4 测。**剩 NICE**：boost 权重(12)可调/可学;可让 fit 也参与跨灯层(目前严格灯序优先,刻意保守)。
   - **B 可靠性工程新族 reliability**（subagent 建 + 主脑集成/修 bug，domain engineering，→37 族，SPC 工业姊妹域）：weibull_life_analysis(删失 Weibull MLE + lognormal AIC 选优 + B-life/MTTF/概率图,**与 parametric_survival 互补不重复**)、reliability_growth(Crow-AMSAA NHPP)、accelerated_life_test(Arrhenius/逆幂律 + 加速因子 + Ea)。
   - **主脑修真 MUST bug（集成时测试逮到）**：reliability_growth 的 Crow-AMSAA MLE 分母用错——默认 T=末次失效是**失效截尾(Type II)**,正确 β̂=(n−1)/Σ,代码用了 n/Σ(时间截尾式)→系统偏高(测试 β̂=0.66 应≈0.6);**改为按截尾类型分支**(Rigdon&Basu:失效截尾 n−1、时间截尾 n)。另修 inter-arrival 检测不可靠(β<1 时间隔本身单调增→误判)→加显式 `input_type` config、默认累积。
   - **冷审 ZERO MUST**（Weibull 删失 MLE、**lognormal-AIC 同尺度**[Jacobian 仅加失效 pdf 项、删失生存项不加,经确认]、**Crow 修正正确**[对 Rigdon&Basu]、Arrhenius 全对）。2 SHOULD 已修:① kelvin 名检测 token `" k"` 脆弱(不匹配 temp_K)→正则词边界;② 瞬时vs累积 MTBF 措辞"增长中通常高于"仅 β<1 成立→改"改善时高于/退化时低于"。NICE 已修:并列值检测用 `has_decrease`(并列累积值不再误判 inter-arrival)。**剩 NICE**：ALT B10 的 σ_log 为池化(少样本/层会低估,已披露);200-300°C 带 Celsius vs Kelvin 固有歧义(已披露)。
   - **教训复用**：subagent 沙箱禁 py/git(主脑兜底跑测试逮到 Crow MUST bug——再次验证"主脑必自验"价值);估计量按截尾/设计类型选正确公式(分母 n vs n−1)。
0. ✅ **discover 真抓取阶段2·活的趋势引擎（2026-06-25，主脑亲建，平台/自我进化闭环）**：原 roadmap 自标"暂缓(选得准>加得多)"，今解禁并完成（选择器+Web 已成形）。
   - **`catalog/trends.py`**：真抓 **PyPI**(下载量 via pypistats + 在册)/**CRAN**(crandb 在册)/**GitHub**(search 星标+最近活跃)→ 0-100 `momentum`(真数字、log 归一、编辑权重 0.45 下载/0.35 星/0.20 活跃 + CRAN presence bonus，缺组件重归一)。镜像 R 桥：可选+优雅降级(离线/无 requests/限流→available=False/momentum=0/回退)+磁盘缓存(`~/.researchforge/trend_cache.json` 7d TTL)+**绝不进分析运行时**(仅 curation-time)。实跑验证:gradient_boosting=97(xgboost 44M下载/42k星)、spatial_panel=25(仅CRAN)、quantile_forest=64。
   - **discover 集成**：`build_live_fetch_fn` + `score_candidate` 融 momentum 进发现优先级(有 live→0.35nov+0.30pub+0.20pop+0.15mom，否则旧式)；`cli discover --live` 重排 + 显示动量 + 写趋势快照。
   - **喂评分卡(闭环)**：`cli discover --live` 写 `trend_snapshot.json`(by_id + by_family 均值)；`score_method` 经 `_live_momentum` 读快照把 momentum 融进 popularity(`0.5*pop+0.5*mom`)+诚实披露"流行含实时趋势"/"离线编辑先验"。**热路径只读缓存、零网络**(lru_cache memoize)。实测:ml 族 popularity 80→72(融 momentum 64)。
   - **诚实护栏**：GitHub 按名搜索加**仓库名含查询词校验**(否则丢弃，防同名无关仓库的星标错安到方法上)；ANCOMBC(Bioconductor 非 CRAN)正确标"不可用"不杜撰。
   - **测试**：`tests/test_trends.py` 13 个(mock `_get_json` 确定性 + parse/momentum 单测 + 降级 + 缓存命中 + GitHub 护栏 + 快照→评分卡闭环 + 真网 smoke 可跳)；**conftest autouse `_isolate_trend_artifacts`** 把缓存/快照重定向到 tmp，使全量对 dev 机器是否跑过 `--live` **hermetic**。test_discover 断言加 momentum 键。全量 1085 绿。
   - **剩 NICE/honesty backlog（无优先级，必清）**：① GitHub 含义护栏会误杀 repo 名≠pypi 名的合法库(如 umap-learn↔lmcinnes/umap 被拒)——宁可少算不错安，可加 SEED 显式 repo 提示提精度；② pypistats 易限流→downloads=None(降级到星标动量，已披露但可加重试/备源);③ CRAN 404 与网络故障不可区分(都→None,detail 笼统说"不可用");④ momentum 权重为编辑值(基于真数据)可调/可学;⑤ 快照只覆盖被发现候选+其族——catalog 里无同族候选的方法拿不到 live popularity，可扩展为按 catalog 族探代表包;⑥ GitHub 未鉴权限流 60/hr(search 10/min)靠缓存缓解，可接 token。
0. ✅ **v1.5 聪明选模做深 + 临床/SPC 双领域波（2026-06-24，3 并行 subagent + 主脑亲建诊断引擎）**：1 主脑（诊断引擎）+ 2 builder subagent（epi/spc）+ 3 冷启动 inference-reviewer。
   - **A 旗舰·自动诊断→带方案推荐**（`recommender/diagnostics.py` 主脑亲建）：跑真实取值诊断（过离散 φ=var/mean+Pearson χ²、零膨胀 vs exp(−x̄)、正态偏度/峰度门控、Koenker BP 异方差、VIF 共线、离群、小样本）→映射「条件→更优方法」（如过离散→negbin 弃 poisson）。**复用 slice-1 的 `likely_outcome` 选诊断 outcome**（两片协同）。接 CLI recommend + web payload + index.html 渲染。冷审 **ZERO MUST-FIX**，应用 2 SHOULD（红线）：① 零膨胀措辞改诚实「零比例高于 Poisson 预期（可能零膨胀也可能仅过离散）」+ prefer 把 zinb 提前（NB 过离散也多零，原 headline 过强）；② 宽 `except Exception` 收窄到预期异常类型（它正吞掉了我先前的 `.any()`-on-scalar 真 bug——自动化红线）。**剩 NICE**：过离散无条件检验披露已加；可加「诊断置信度」分级；可把诊断结果喂进 recommend 排序（现仅 advisory，不改排序/默认）。
   - **B 临床/流行病学波 epidemiology**（新族，→domain/family epidemiology）：`diagnostic_test_eval`（ROC/AUC+DeLong CI、Youden 切点、sens/spec/PPV/NPV/LR±+Wilson CI）/`epi_risk_measures`（2×2 RR/OR/RD/NNT/AR%/PAR%+Katz/Woolf/Wald CI、Haldane-Anscombe）/`calibration_assessment`（Brier+Murphy 分解、Hosmer-Lemeshow、校准斜率/截距）。冷审 **ZERO MUST**（DeLong 用暴力实现逐位核验、AUC=MW 恒等、HL df=g−2、全 Rothman 公式、校准斜率 Cox1958 都验过），应用 2 SHOULD（诚实红线）：① 评分翻转后写明判定方向「原始评分 ≤ 切点 判阳」（否则用户判反）；② 完美区分 AUC CI 退化为点估计标 ⚠。**剩 NICE**：Murphy 分解用等宽箱 vs HL 用分位箱（披露或统一）；PAR%(Levin) 对 case-control 设计的样本暴露率代表性披露。**主脑修 2 个测试断言 bug**（实现对）：RR CI 下界 0.986 是正确 Woolf（该 2×2 恰擦不显著，测试误设 `>1.0`）；AUC 4dp 舍入 vs 1e-6 容差→实现改 6dp 舍入 + `np.trapz`→`getattr(np,"trapezoid",np.trapz)` 清弃用警告。
   - **C SPC 质量工程波 spc**（新族，→domain quality/family spc）：`control_chart`（Shewhart X-bar&R / I-MR + Western Electric 判异）/`process_capability`（Cp/Cpk/Pp/Ppk + ppm + 单侧规格）/`gage_rr`（AIAG ANOVA 法量具 R&R：两因子交叉随机效应 EMS 方差分量、p>0.25 交互合并、ndc=⌊1.41·σ_part/σ_grr⌋）。前两者确定性（Shewhart 常数表 + 正态 CDF，实测验证）；gage_rr 真推断冷审 **ZERO MUST-FIX**（EMS 方差分量逐项对齐 Montgomery/AIAG 表、p>0.25 合并后用 MS_err 重算正确、typ=2 对平衡设计不失真、%Contribution(和≈100%) vs %StudyVar(平方和≈100%)未混淆、ndc=1.41·σ_part/σ_grr、负分量截断有披露）。**剩 NICE**：死变量 `nm`(可简化为 any 推导)；emitted code 片段只示全模型分支(可注合并路径)；分量为 MoM 点估计无 CI(可一句披露)。**主脑修 YAML bug**：spc.yaml 有 `- %Contribution...` 裸标量 `%` 起头 safe_load 崩→引号化（catalog 守卫当场抓回）。
   - **教训复用**：subagent 沙箱仍禁 py/git（主脑兜底跑测试+修 bug+提交）；YAML `%`/`{`/`:` 起头或内含必引号（守卫已挡）；estimates 是 `dict[str,float]`，CI 拆标量键（已在 spec 注明）。
1. 🔄 **并行 subagent 集成队列（2026-06-19，compact 后用 `git worktree list` 找回）**：
   - ✅ **设计流（组件库）**：已审+合并+推送（main 6b267b8，web/static/components/ 8 文件）。**待办：DesignSync 推到 claude.ai/design**（需用户登录）。
   - ✅ **农学设计波（已审+整合 6/19）**：`ammi`/`gge_biplot`/`response_surface`（纯 Python，experimental_design.py 1230 行）。inference-reviewer **APPROVE**——手搓 SVD/canonical 数学经对数核验全部正确（AMMI 双中心化+秩 min(g-1,e-1)+ASV 方向；RSM 驻点 -0.5B⁻¹b + Hessian 特征值分类；GGE 列中心化+which-won-where 投影）。已合并 main，全量 292 绿。**剩 nice-to-have（无优先级，必清 backlog）**：(a) AMMI 中 `n_axes>=2` 守卫因前置 g,e≥3 永真、可简化（无害）；(b) AMMI `interaction_norm` 是 `inter` 行 Frobenius 范数（原始交互幅度，与 catalog 标签"interaction-residual norm"一致，仅记录）。
   - ✅ **空间面板（已审+整合 6/19）**：`spatial_panel` SAR/SEM/SDM（R splm，个体 FE within）。inference-reviewer **APPROVE-WITH-FIXES**——核心数学(LeSage-Pace impacts S=(I-ρW)⁻¹(Ib+Wθ)、`mean(rowSums)=1/(1-ρ)` for 行标准化 W、W↔数据单位排序、SDM Durbin、SAR/SDM ρ 提取)实测 R 4.6.0/splm 1.6.5 全部核验正确。**已修 MUST-FIX**：SEM(model="error")的 λ 恒报 0.0000——within FE 下 `m$errcomp` 为 NULL，误差参数 `rho` 实住 `m$coefficients`；改按模型分支提取 + 加 SEM 回收测试。已合并 main，全量 298 绿。**剩 backlog（无优先级，必清）**：(a) SHOULD-FIX：LM lag-vs-error 用非稳健 `lml/lme`（两者对任一依赖都拒），换稳健 `rlml/rlme` 判别更准（仅 advisory，默认按 config）；(b) NICE：lon/lat 无名匹配时回退 `geo[-1]/geo[0]` 可能轴互换（欧氏 k-NN 影响小、可 config 覆盖），可加回退告警；(c) impacts 仅点估计、无模拟 SE（splm impacts() 跨版本碎，已披露"未附 SE/条件于点估计"），补 delta/bootstrap SE 仍待做。
   - ✅ **生态波（已审+整合 6/19）**：`mantel_test`/`indicator_species`（纯 Python）+ `rda`（R vegan）。inference-reviewer **APPROVE**——Mantel r 匹配 scipy 到浮点精度 + 联合行列置换是正确 Mantel 零分布且校准良好；IndVal A×B×100 完美匹配 Dufrene-Legendre(1997)（完美指示种=100、均匀=低）；RDA 端到端匹配直跑 R（total/constrained/F=274/p=.001 完全一致）。已合并 main，全量 307 绿。**已应用 3 个 nice-to-have**：(a) RDA 真给调整 R²(RsquareAdj，原只提示)；(b) Mantel summary 补空间自相关告警；(c) IndVal summary 补稀有类不稳告警。剩：无（#2/#5 经核为非问题）。
   - **整合顺序（全部完成 ✅ 6/19）**：✅① 农学 a4079af → ✅② 空间面板 a0ee0a3 → ✅③ 生态 a5c9ed1。三波 inference-reviewer 双审（空间面板抓 1 个 SEM λ MUST-FIX 已修+回归测试，农学/生态 APPROVE），逐个 merge + 全量绿 + push。全量 283 → **307 绿**（+9 农学 +6 空间 +9 生态）。**剩唯一待办：DesignSync 推组件库到 claude.ai/design（需用户登录）。**
   - 编排约定见记忆 [[design-tooling-and-orchestration]]：worktree 隔离、不相交文件、主脑双审、push 等用户。**红线：未经 inference-reviewer 审的真推断方法不合并/不推。**
1b. ✅ **并行 subagent 集成队列 — 第二批（2026-06-19，全部完成）**：DesignSync（组件库 8 文件推 claude.ai/design 项目 "ResearchForge UI" 41510f35，卡片由 @dsCard 标记自动建）+ 3 方法波。**本批环境注意**：建造 subagent 的沙箱**禁止跑 Python/git**（与第一批不同），故 3 个 subagent 都没自验测试也没提交——主脑兜底跑测试 + 提交 + 修 bug（见 [[design-tooling-and-orchestration]]）。
   - ✅ **生存波**：`competing_risks`（lifelines AalenJohansen CIF + 可选 R cmprsk Gray 检验）/`parametric_survival`（Weibull/LogNormal/LogLogistic AFT，AIC 选优）/`rmst`（受限平均生存时间 + 组间 z 检验）。inference-reviewer **APPROVE**。**主脑修 1 个真 MUST-FIX**：RMST 组间 SE 误用 lifelines `restricted_mean_survival_time(return_variance=True)`（那是受限寿命**分布**方差 ~θ²，不随 n 收缩），致 SE 大 ~11×、p=0.607 掩盖真实差异（真 p≈6e-9）；改用 Klein-Moeschberger/survRM2 估计量方差（新 helper `_rmst_with_se`），经 R `survival::survfit` 核验到 6 位小数。**剩 nice-to-have**：competing_risks 头条 CIF 是 pooled（summary 可注明）；AFT 可改用 lifelines 原生 `exp(coef)` CI 列（数值同）。
   - ✅ **MCDA 波**：`entropy_weight`（Shannon 熵客观赋权）/`vikor`（折衷排序 + C1/C2 接受条件）/`promethee`（PROMETHEE II 净流，V 型偏好）/`ahp`（层次分析特征向量权重 + 一致性比 CR；config pairwise 专家判断，缺则数据代理并披露）。确定性纯 numpy，18 测断言手算值绿；AHP CR 对数核验正确（测试不一致矩阵原 CR=0.056 太一致、已修为 CR=0.117）。
   - ✅ **多元波**：`manova`（四统计量 Wilks/Pillai/HL/Roy）/`discriminant_analysis`（LDA/QDA + 分层 CV 准确率）/`canonical_correlation`（白化 SVD 典型相关 + Bartlett/Wilks 序贯）/`hotelling_t2`（两样本 T²→F）。inference-reviewer **APPROVE**——Hotelling T²→F + CCA Wilks 序贯经数值核验正确（对比 sklearn CCA / 特征分解）。**已应用披露润色**：CCA 小样本上偏入 summary、MANOVA "Box's M" 措辞不暗示已检验。
1c. ✅ **并行波3 + 审计（2026-06-20，全部完成，414 绿）**：4 波各派 inference-reviewer（全 APPROVE/-WITH-FIXES）。**沙箱又禁 subagent 跑 Python/git**——主脑兜底跑测试时抓到多个真问题（见下），并发现 finance.yaml 的 YAML 语法错（描述含 `: ` 未引号，subagent 没跑全 catalog 故漏）。**主脑修的真 bug/问题**：① 状态空间 markov `res.param_names`→`res.model.param_names`（拟合即失败）；② 状态空间 DFM 传 `Z.to_numpy()` 丢列名→载荷全 NaN→方差解释塌成 0；③ 金融 EVT ξ→0 分支比率写反（高估 ~166%，inference-reviewer MUST-FIX）；④ 金融价格检测加滞后1自相关（小漂移价格漏判）；⑤ 状态空间 DFM 方差解释改 1-σ²（平滑因子方差弱信号低估 30-60%）；⑥ 非参 BCa z0 改 tie-aware（离散统计量方向错）+ 退化守卫；⑦ 心理测量 min_numeric_cols 预条件（Likert/count 数据原永不被推荐）+ omega 标准化α 键改名；⑧ finance.yaml YAML 修。**剩 backlog（无优先级，必清）**：
   - 审计未修项：csQCA/fsQCA "二值列直接用"文案与"仅选连续列"实现不符（R-gated）；`guidance_only` catalog 标志（iv_regression/pls_sem/bayesian_sem 是 guidance-only 却被当可跑推荐）；survival_analysis 把 matplotlib 当硬依赖（plot 未隔离）；GARCH omega 缩放单位披露；spatial_panel predictors `[:5]` 优先级。
   - 波3 nice-to-have：心理测量 ICC 死守卫(`not in (0,nan)`)/Feldt CI 未夹≤1/p1 单因子F 未导出/omega_hierarchical(多维);金融 n_tail_obs 只反映历史/CF 非单调披露;状态空间 N1 冗余 smp 分支/N2 死 markov 回退/N3 全NaN nanmean 警告守卫;非参小样本 BCa(<20) 量化告警/置换精确性注;心理测量 pilot/全不同整数 id-trap 鲁棒（小样本量表）。
   - ✅ **自愈护栏（已做，波③ 1h）**：`tests/test_catalog_yaml_valid.py` 逐个 safe_load 所有 catalog/entries/*.yaml + AnalysisEntry 构造 + 重复 id 检查，YAML 语法错快速失败点名，根治 51 级联。
1d. ✅ **并行波4（2026-06-20 下午，全部完成，473 绿/125 方法）**：4 波各派 inference-reviewer（计数 APPROVE;GWR/贝叶斯/一致性 APPROVE-WITH-FIXES）。GWR 这个 subagent 沙箱允许跑 py 并自验+提交;其余沙箱禁、主脑兜底。**主脑修的真 bug/问题**：① GWR 非平稳判据 100% 误报→降格为描述性指标+诚实披露;② 贝叶斯 `P(θ>ref)` 用 1-cdf 上尾抵消成 0→改 sf;③ 贝叶斯泊松过离散标志在原始计数算、异质曝光假阳→改曝光感知 Pearson 离散度;④ 一致性 requires_group 让数值评分码永不被推荐→加 `min_categorical_cols` 预条件;⑤ Fleiss 不等n 对齐中位数→众数;⑥ tweedie 预条件 requires_count_outcome→min_continuous;⑦ 多处测试数据/容差(贝叶斯等臂/ref seed、舍入容差)。计数 ZIP/ZINB inflate_/alpha 提取、GWR tr(S)/AICc/大带宽→OLS、贝叶斯 P(B>A)/HPD、一致性 加权κ/Fleiss SE/LoA CI 均经 reviewer 数值核验(对 sklearn/statsmodels/MC)。**剩 nice-to-have（无优先级）**：计数 row-floor 对齐 catalog min_rows / NaN-in-estimates JSON / 协变量感知 expected-zero;GWR 自适应 bisquare 第 k 邻权重为 0 的 off-by-one、tr(S) df 定义注;贝叶斯 HPD 网格扫近似、AB 抽样数披露;一致性 加权κ 无 SE 披露、Fleiss 小 N 的 z 渐近告警、丢弃被试数披露。**新增预条件字段** `min_categorical_cols`(categorical+binary+count)已入 schema+match（与昨日 `min_numeric_cols` 同模式）。
1e. ✅ **并行波5 + 装包（2026-06-20 晚，520 绿/133 方法）**：用户批准装包——已装 **girth/stepmix(py) + mirt/did/poLCA(R 二进制,经 rbridge `find_rscript()` 路径装,无需编译器)**；synthdid 未装(GitHub 源码需编译器,跳过)。4 波：IRT(girth)/潜类别(stepmix)/混合(sklearn)/Callaway-DiD(R did)。**⚠ subagent 审者撞会话额度上限(1:10am Adelaide 重置)**→改**主脑亲审**(审者≠builder)+ 各波已知真值 recovery 测试为据。**主脑修的真 bug/问题**：① IRT `_binary_item_matrix` 返回 float 致 girth "arrays used as indices" 全失败→改 int;② IRT 2PL 回收测试 n 600→1000(判别力回收噪声大 r_a 0.71→0.93);③ LCA 测试硬编码输出路径(无时间戳)→改 res.output_dir;④ LCA 3类 ARI 0.8→0.7(class2 与 class0 仅 2 项差异、0.76 已 substantial);⑤ 混合 mahalanobis 测试 _run 嵌套目录未 mkdir。主脑核验:Mahalanobis chi2.ppf(.975,df=p)×MCD=Rousseeuw 标准;LCA `_order_by_size` 标签对齐 pis/proba/weights/labels 同 perm 一致;IRT girth 朝向(r_a.93/r_b.98);Callaway R recovery(ATT≈τ/预趋势≈0)端到端验。**✅ 冷启动 reviewer 已补跑（额度恢复后）**：4 个独立 inference-reviewer 全部确认主脑审无误——IRT/混合 APPROVE-WITH-FIXES、LCA/Callaway APPROVE。核心数学独立数值核验全对（IRT girth 朝向 r_a=.96/r_b=.998 + EAP 参序、LCA gaussian_diag 实为必需(gaussian_unit→covariances=None 崩)+ 标签对齐强制置换验、Mahalanobis df=p + 尺度不变 2e-14、Callaway 对 live did v2.5.0 全 slot+recovery ATT≈τ）。**已修 SHOULD-FIX**：① IRT "改看 2PL/Rasch 不成立"自动判语对真 Rasch 误报~27%（plug-in loglik 差非 LR 检验）→改中性措辞；② 混合 MCD χ² 阈有限样本反保守（干净数据~4.4% vs 名义2.5%，Hardin-Rocke）→披露点明为上界；③ Callaway 派生 gname 对非吸收(开关式)处理静默当吸收→加 ⚠；④ LCA LPA BIC 在标准化数据上算→披露绝对值不可跨模型比。**剩 nice-to-have（无优先级）**：IRT 多分类(grm/pcm girth)、person-SE 真值、separation-reliability 短测常 NaN 披露、θ EAP 收缩(SD<1)措辞、loglik_2pl 用 nan 哨兵；LCA min_rows catalog(50)↔branch(20) 对齐或注、多分类 multinoulli profile 路径加固;混合 DBSCAN 无肘数据 eps 兜底、GMM 原始量纲均值列;Callaway 前趋势 pointwise→cband 同步、att_dynamic_overall summary 注。
1f. ✅ **并行波①（2026-06-22，573 绿/144 方法）**：装 prince + python-louvain。4 波：因果敏感性(纯Py)、多项 IRT+DIF(girth)、降维(sklearn+prince)、网络科学(networkx+louvain)。**主脑兜底跑测试 + 冷启动 inference-reviewer 双重确认**（额度恢复后补跑，4 审全回）。**主脑兜底修的真问题**：① 网络 Louvain `weight=None` 触发 python-louvain `**{None:..}` 崩→`wkey=(列名 or "weight")`；② rosenbaum_bounds **设计缺陷撤除**（按输出值排序配对做 signed-rank=循环、Γ 虚高）→ **已在波②正确重做**（协变量匹配对 + signed-rank Γ 边界，见 1g）；③ 一串测试数据/容差（IRT int 矩阵已上一波修、evalue n、SIR 守恒 atol、LCA 路径等）。**冷审 MUST-FIX（已修）**：① Oster 负 δ 判语区分抑制子(稳健)vs 穿零null(脆弱)；② CA/MCA total_inertia 用满秩拟合(满足 χ²/n)；③ irt_pcm 据实标为 GPCM(girth pcm_mml 自由每题区分度,非等区分度 PCM)。**冷审 SHOULD-FIX（已修）**：网络 centrality 权重语义披露 + closeness 用 distance=w。核心数学冷审独立数值核验全对（Oster δ/β*、E-value、girth GRM 朝向 r=.99、MH-DIF vs statsmodels 精确、MDS stress-1、CA inertia=χ²/n、PLS VIP mean²≈1、SIR 守恒/单调）。**剩 nice-to-have**：Oster β* 简化界 vs psacalc 三次解、evalue 连续暴露中位二分披露;IRT GRM 严格递增措辞、PCM EAP 模型近似披露;CA cumulative_pct/非度量 MDS 用 Spearman;网络权重距离反转更深处理、SIR R0 用 <k²>/<k>。
1g. ✅ **并行波②（2026-06-22，623 绿/153 方法）**：装包无新增（纯 Python scipy/statsmodels）。3 并行 builder subagent（沙箱禁跑 py/git，主脑兜底跑测试+提交+修）+ **主脑亲建 rosenbaum 重做**。3 family：列联表 categorical（`loglinear`/`cmh_test`/`ordinal_association`）、分布拟合 distribution（`distribution_fit`/`goodness_of_fit`/`qq_analysis`）、调节过程 conditional_process（`moderated_mediation`/`johnson_neyman`）。**rosenbaum_bounds 已正确重做**（撤波①的循环缺陷）：协变量匹配对（倾向得分 logit 最近邻 + 0.2·SD caliper，标准化协变量距离回退，**绝不按结果配对**）+ signed-rank Rosenbaum Γ 边界；冷审实测验证（Γ=1 与 scipy.wilcoxon 一致、边界方向单调、确认协变量匹配）→ **零 MUST/SHOULD**。**主脑兜底修的真 bug**：① categorical `_categorical_like_cols` 误把全不同整数(id-trap)/高基数列当列联表因子→加低基数上限(自动选择时,config 仍可强指);② QQ 测试期望错(实现对：对拟合分布的 Q-Q 斜率≈1/截距≈0,非数据 SD)。**4 冷启动 inference-reviewer 全回**——conditional_process **1 MUST-FIX 已修**：moderated_mediation bootstrap 每次重抽用各自样本 W 均值重新中心化,但评估条件间接效应却用全局 w_mean,锚点不一致→给条件间接 CI/网格带注入虚假方差(±~30-50% 多余离散);改 `_fit_indices` 返回每次锚点、bootstrap 内一致评估(index CI 与点估计本就不受影响)。categorical/distribution/rosenbaum **无 MUST**。**已应用的 SHOULD-FIX（honesty 红线）**：categorical ① 残差实为 Pearson 残差却标「标准化」且套 |·|>2≈N(0,1)(无效)→改算真·调整残差 (O-E)/√(E(1-row/n)(1-col/n))(使断言有效);② γ 的 z 检验用了 CI-SE(ASE1)而非零假设 SE→去掉误校准 gamma_p,改用 ASE1 报 γ 的 95% CI(其正确用途),显著性指向 τ-b 的 p(同一 null);③ Breslow-Day 未加 Tarone 校正→披露。distribution ① AD 的 scipy 1.17 FutureWarning→catch_warnings 抑制 + 防御读临界值;② CvM 也用估参 cdf 同样保守→扩展披露(原只提 KS);③「Filliben PPCC」名实不符(实为 Hazen 绘图位,非 Filliben 中位秩)→改名 PPCC(Hazen)。冷审独立数值核验全对（G²/CMH χ²/MH OR/Breslow-Day/τ-b/Somers' D、MLE+AIC/BIC、KS/AD/CvM、JN θ(w) 方差+二次边界、PROCESS model 7 index=a3·b、Rosenbaum Γ 边界）。**剩 nice-to-have（无优先级，必清）**：
   - categorical：loglinear `goal:describe` 但报 p(措辞);ordinal `goal:describe` 同;cmh 连续性校正硬开(可暴露未校正版对比);config 拼写错静默回退自动(可告知"你的 X 被忽略")。
   - distribution：AD significance_level 单位守卫(若未来 scipy 改用分数 0.05 则 `5.0 in sigs` 落空→可加 `max(sigs)<=1` sanity guard);delta_aic=10 决定性阈是经验法则(可披露);bins="auto" 重尾退化(已 try/except 兜底)。
   - conditional_process：JN summary 未说显著区在边界内/外(CSV/PNG 的 sig 标志已带,可补一句);config 拼写错静默回退(同上)。
   - rosenbaum：`gamma_critical=NaN` 语义重载(既"Γ=1 已不显著"又"极稳健 Γ≤6 不破")——verdict 文案已区分,可加 `gamma1_significant` 标志或робуст时设 inf;标准化回退路径无 caliper(已披露,可补 0.2·SD caliper)。
1h. ✅ **并行波③（2026-06-22，690 绿/162 方法）**：纯 Python（statsmodels/lifelines/linearmodels，无新装包）。3 并行 builder subagent（沙箱禁跑 py/git，主脑兜底）+ **穿插清 3 件 backlog**（主脑亲做）。3 family：现代 DiD 稳健性 did_advanced（`goodman_bacon`/`honest_did`/`chaisemartin_did`）、生存补全 survival_extra（`time_varying_cox`/`stratified_cox`/`cox_ph_diagnostics`，lifelines）、面板补全 panel_extra（`mundlak`/`hausman_test`/`first_difference`，linearmodels）。**主脑兜底修的真 bug**：① panel `hausman_test` 在 V_FE−V_RE 非正定（有限样本 V_FE<V_RE）时经典式失效、会把 H 截 0 → 误判"用 RE"（实测 b_FE=1.51 vs b_RE=1.61、vdiff=−0.00027）→ 改为 **PSD 门控 + 回归式(Mundlak)Hausman 回退**；② cox_ph_diagnostics 测试的非比例 DGP 把 x_np 效应键在随机半数而非时间上（无时间趋势,Schoenfeld 检不出）→ 改两相分段指数（x_np HR 在 tau=0.4 后消失,真造非比例,p_np=2e-27 vs p_p=0.29）。**4 冷启动 inference-reviewer 全回——三族全无 MUST-FIX**（我的 hausman 回退获独立确认统计正确、是真改进）。**已应用的 SHOULD-FIX（honesty/正确性）**：did ① Goodman-Bacon summary 把对账缺口误归因"面板不平衡"（平衡面板上实为简化权重形）→ 据实改 + `twfe_did`→`twfe_did_decomp`（分解和≠真 TWFE 系数,真系数 `twfe_did_direct`）；② honest_did 的 `max_pre` 从系数**水平**改为 pre 期**一阶差分**（RR 相对幅度约定,与 HonestDiD 包 M̄ 同尺度）；panel ③ hausman 回退改用**无调整**协方差（与经典支同基准,clustered 稳健版留在 mundlak）+ mundlak"等价"措辞收紧；survival ④ time_varying_cox catalog 误称有 concordance（CoxTimeVaryingFitter 无 C-index）→ 删除。冷审独立数值核验全对（Goodman-Bacon 三类比较+坏权重>0、honest_did 事件研究+breakdown 公式、DID_M 稳定对照+单位 bootstrap、时变/分层 Cox+Schoenfeld、Mundlak Wald、Hausman 回退、FD 估计）。**穿插清的 3 件 backlog**：① **catalog YAML safe-load 护栏**（`test_catalog_yaml_valid.py` 逐文件 safe_load+schema 构造+重复 id,坏文件快速失败点名,根治 51 级联教训）；② **sharp rdd 结果列守卫**（`config["outcome"]` 误设为 running 列时不再接受,回退自动选 ≠running 的连续列,与 fuzzy_rdd 一致）；③ **distribution AD sig-level 单位守卫**（未来 scipy 改用分数 0.05 也能正确匹配 5% 临界值）。**剩 nice-to-have（无优先级，必清）**：
   - did_advanced：goodman_bacon 升级为精确 bacondecomp 权重（当前简化形,相对结构可靠、绝对值略偏,分解和与直接系数不强行对齐）；honest_did 补完整 polyhedral/FLCI + 平滑度(二阶差分)变体；三族均未单独点明 **no-anticipation** 假设（隐含,可补一句）；goodman_bacon 测试 docstring 已更新。
   - survival_extra：time_varying_cox `min_ph_p` 用舍入后 p 而 violates_PH 用原始 p（0.0499 边界显示不一致,可统一从未舍入 p 算）；time_varying 自动检测可能把 binary 化的 start/stop 当协变量（stop>start 守卫已诚实拦截,可在报错提示用 config）；time_varying 补独立删失假设披露。
   - panel_extra：hausman pinv 支若二次型 < −1e-8 应升 ⚠ 而非静默截 0（PSD pinv 下不会触发,低优）；`_panel_frame` 的 drop_duplicates 静默丢真重复观测（家族级约定,可加 ⚠）。
1i. ✅ **并行波④ + 工程债清除（2026-06-23，171 方法）**：纯 Python（statsmodels/numpy/scipy/networkx，无新装包；pysal/esda 未装→手搓）。3 并行 builder（沙箱禁跑 py/git，主脑兜底）+ **主脑亲做工程债：`causal.py`(1461 行,头号金丝雀)→ `branches/causal/` 包**（一方法一模块、13 个、最大 205 行、`pkgutil.walk_packages` 递归自动注册；用 `_split_causal.py` 一次性脚本程序化切分=零手抄误，跑后删除）。3 family：空间补全 spatial_extra（`ripleys_k`/`getis_ord`/`join_count`，手搓 numpy/scipy/networkx）、回归诊断 regression_diagnostics（`vif_multicollinearity`/`heteroskedasticity_test`/`influence_diagnostics`）、相对重要性 importance_analysis（`dominance_analysis`/`relative_weights`/`commonality_analysis`）。**主脑兜底修的真 bug**：① 2 个 spatial degrade 测试 `any("跳过" in s for s in res.summary)`——summary 是字符串、迭代成字符恒 False（importance builder 自己避开了、spatial 没）→ 改 `in res.summary`；② commonality CSV 求和容差（舍入累积 1e-6）。**3 冷启动 inference-reviewer 全回——三族全无 MUST-FIX**（importance 三法 sum-to-R² + Johnson P 矩阵 + commonality Möbius 全经手工符号验证；spatial join-count 方差被 reviewer 从头重推无误；regdiag het 元组序/influence slots 全对）。**已应用的 SHOULD-FIX（honesty/正确性）**：spatial ① 全局 Getis-Ord G 只对**非负**变量有定义→加 `x.min()<0` 跳过守卫 + 披露（Gi* 不受影响、任意实数可用），测试改用非负 pollution 场；② join-count WW 补 z 检验（与 BB 对称 p↔q）；regdiag ③ 条件数从"仅预测变量"改为**含截距的全设计**(BKW 规范,>30 阈才适用)；④ White 辅助回归 df 从名义式 `p+p+p(p-1)/2` 改为**实际秩**（二值预测变量的平方=自身会被丢、名义式高估）。冷审独立核验全对。**剩 nice-to-have（无优先级，必清）**：spatial Ripley 边校正披露可注明采样角数(36)；regdiag `mean_vif` 全非有限时→inf 宜改 nan、het 结论以 BP 为准宜注明、影响点 obs 为去缺后位置(非源索引)宜带 `sub.index`、补一个二值预测变量测试覆盖 White-df 边界；importance config 强指 outcome/predictor 被静默覆盖/丢弃宜提示一行。
1j. ✅ **并行波⑤ + v0.8 里程碑（2026-06-23，762 绿/180 方法/v0.8.0）**：纯 Python（statsmodels/sklearn/scipy，无新装包）。3 并行 builder + 主脑攻 v0.8 Web。3 family：有序回归 ordinal_regression（`proportional_odds_logit`/`ordered_probit`/`brant_test`）、密度尾部 distribution_extra（`finite_mixture`/`kernel_density`/`tail_index`）、抽样设计 survey_methods（`weighted_estimation`/`poststratification`/`raking`）。**v0.8 重大发现（诚实 pushback）**：Web MVP **本就完整**（app.py 全 API + static/index.html 366 行单页 + `cli web` + test_web）——"可用性 58" 是 **scorecard 探测错路径**（查 `web/templates/` 而 UI 在 `web/static/index.html`）。诚实修探测→**可用性 58→83、总分 90→93**；补真缺口 `/api/run` 接 config 覆盖（+测试）；**版本 v0.7.0→v0.8.0**、`__next_milestone__`→v0.9、roadmap 阶段①勾。**主脑兜底修真 bug**：① ordinal builder 撞 `ordered_logit`(已存于 statistics.py)→用 `proportional_odds_logit`,其 test_ordered_logit.py 实测新 id→**重命名 test_proportional_odds_logit.py**(不覆盖旧的);② distribution kernel_density 测试列长不齐(junk300/target400)pandas 崩→改齐;③ distribution tail_index PNG 标题用了中文 tail_desc(豆腐块)→改英文 tail_en。**3 冷启动 inference-reviewer 全回**——**2 个 MUST-FIX(均先实测验证再改)**:① ordinal 阈值行报的是 OrderedModel **未变换的优化器空间参数**([c1,log(c2-c1),…]),非真切点→主脑实测 `mod.transform_threshold_params` 确认(reviewer 把方法挂在 results、实际在 model,实测纠正)→3 处修(两新 handler + **同步修 statistics.py 旧 ordered_logit** 同一潜伏 bug),切点 SE 是增量尺度故置 nan+披露;② survey weighted_estimation 谎称"给 group 就分层/PSU 聚类 SE"(实际 group 仅出分组描述均值、SE 不变)→改措辞(2 py + yaml)。**已应用 SHOULD**:distribution "Scott" 带宽实为 Silverman 1.06 normal-reference→改真 Scott(1.0·sd·n^-1/5=scipy 默认)。**穿插清 backlog**:regdiag `mean_vif` 全非有限→nan(不再 inf)。三族其余数学冷审独立核验全对(proportional-odds OR/McFadden/LR、probit 潜尺度、Brant 保守近似(忽略跨切正协方差→低估拒绝=保守,已披露)、Hill 公式+稳定区、KDE bw 换算、finite_mixture BIC、HT 均值/deff/n_eff、poststrat 因子、raking IPF 保总量)。**剩 nice-to-have（无优先级）**：ordinal 切点 SE delta 法、Brant 全局 χ² 跨预测变量独立性近似措辞、数值 Likert 回显排序;distribution Scott vs scipy 默认核对(已对齐)、finite_mixture BIC 缓存/k=1 失败注、KDE 涟漪滤波;survey poststrat 空总体格跳过/告警、整数权重列自动识别、raking deff 措辞、weighted SE 对金标准(R survey)固定;regdiag 条件数已修、het 结论以 BP 为准宜注、影响点 obs 用源索引、补二值预测变量 White-df 测试。
1k. ✅ **并行波⑥（2026-06-23，809 绿/189 方法，折入 1.0 经典统计/非参/ML）**：纯 Python（statsmodels/scipy/sklearn，无新装包）。用户选「继续方法波」（非 v0.9 硬化）。3 family：经典 ANOVA experimental_stats（`anova_oneway`/`ancova`/`repeated_measures_anova`）、非参组检验 nonparametric_tests（`kruskal_wallis`/`friedman_test`/`mann_whitney`）、监督 ML ml_supervised（`regularized_regression`/`svm_model`/`gradient_boosting`）。**主脑兜底修真 bug**：① **`ml_supervised.yaml` 的 biases 写成 `>-` 折叠标量(字符串)而非 list → `Catalog.load()` 崩 → 49 测试级联失败**（横扫 catalog/cli/web/recommend/discover…）；**`test_catalog_yaml_valid` 护栏(波③做的)精确点名该文件**，改 biases 为列表即解；②③ ml 分类检测（引擎惯例首连续→回归，分类目标须 config outcome）→分类测试加 config；lasso 稀疏（默认 elasticnet 不保证归零→改 method=lasso + 15 噪声列）；kruskal h_stat 容差（handler 4dp 舍入 vs 测试 1e-6→改 1e-3）；ordinal 撞已存 ordered_logit→用 proportional_odds_logit 测试改名（注：这是上一波波⑤的，1j 已记）。**3 冷启动 inference-reviewer 全无 MUST-FIX**——experimental 的 Welch-df2 + GG-ε 被 reviewer **用 rbridge 对 R oneway.test/mauchly.test 逐位核验一致**、effect size 对教科书 viagra 数据集匹配；ml 的 **StandardScaler 在 CV pipeline 内（无泄漏）+ 指标全留出样本 + 嵌套选 α + 置换重要性在留出集** 经代码核验；nonparam 的 rank-biserial 符号/Dunn tie-z/Nemenyi CD(√2 折入)/Kendall W/HL CI 全手工核验。**已应用 SHOULD-FIX**：① experimental ANCOVA 协变量键用精确 `Q("名")` 匹配（防子串误配）；② nonparam ε² 实为 η²[H]（rstatix eta2[H]，非 Tomczak ε²=H/(n-1)）→重命名 estimate 键 `epsilon_squared`→`eta_squared_h` + 改注释/引用/标签（handler+catalog+test）；③ ml 把"分类须 config outcome"+"置换重要性单留出集小n不稳"披露折入 yaml biases。**剩 nice-to-have（无优先级）**：experimental Mauchly 补 Box 二阶项(GG 已精确、仅 Mauchly p 边界)、RM 提供 generalized η²、ANCOVA 斜率齐性用联合 F；nonparam studentized_range 旧 scipy 守卫、KW 提 Holm/BH 更紧;ml gradient_boosting 置换重要性可改 CV 内计算、svm n_sv 标为全拟合描述、移除 dead `min_rows` 参/未用 import。**教训固化**：catalog yaml 的 `biases`/`produces` 必须是 **list**，描述才用 `>-`；幸有波③护栏点名（否则 49 级联难定位）。
2. **discover 真抓取（阶段2）**：`catalog/discover.py` 现离线 SEED，已留 `fetch_fn` 注入点；接真实 CRAN/PyPI/GitHub，带诚实降级；流行度/更新喂回 `recommender/scoring.py`。
3. **更多方法波**（域聚焦 生态/农学/经济/环境/社科）：农学实验设计(AMMI/GGE/RSM)、生态(RDA/CCA/SDM)、MCDA(AHP/熵权/VIKOR)、合成DiD/de Chaisemartin、贝叶斯状态空间…（见 method-melting-pot-roadmap 记忆）。
   - 〔GAMM nice-to-have：无 config 时结果意图是二值/计数却默认高斯，可加 summary 提示。UX 优化、非 bug。〕

---

## 方法层（method gaps）

| 项 | 为何降级/未做 | 当前替代/现状 | 如何补全 |
|---|---|---|---|
| **bayesian_sem** | 本机无 blavaan、无 RTools 编译器（brms 实测 `make not found`）、无 JAGS；不自动触发重型工具链装 | 诚实降级 → 指向 `sem`（频率派 CB-SEM）/`efa` | 装 `blavaan` + JAGS（或 RTools/Stan）→ 接 `bsem()` 真后验路径（载荷/路径可信区间） |
| **差异丰度 ANCOM-BC** | 专用桥未接（需 TreeSummarizedExperiment 构造） | ALDEx2 金标准已接（`da_method=aldex2`）；ancombc 诚实降级 | 接 ANCOMBC：构 TSE → ancombc2() → 解析 |
| **GAMM 非高斯** | 目前仅高斯族（连续结果） | 高斯 GAMM 已上线 | 扩 binomial/poisson GAMM（mgcv family=）+ 双审 |
| ~~**RDD 模糊断点**~~ ✅ | ~~仅 sharp RDD~~ | **已上线**（2026-06-18）：`fuzzy_rdd`，rdrobust `fuzzy=`，报 LATE + 第一阶段弱工具检测；inference-reviewer 审「correct as-is」 | — |
| **需用户测量模型的 SEM 族** | sem/pls_sem 的结构引擎不能猜 | sem 支持 `config model_spec`；pls_sem 诚实降级 | 文档+示例引导用户写 lavaan 语法 |

## 设计驱动（需 config 指定，非自动）
RDD（running/cutoff）、synthetic_control（treated_unit/treatment_time）、changes_in_changes（treated_group/periods）、joint model（marker/event_time/event）、double_ml/causal_forest（treatment）——这些设计性角色引擎不猜，靠 config（已在 docs/loop-decisions.md 速查 + 各分支诚实降级提示）。**优化**：可加更强的列名启发式 / profiler 增强（如识别"事件时间""驱动变量"列名）。

## 工程层（engineering）

| 项 | 现状 | 优化 |
|---|---|---|
| 全量测试慢（~2–4 min，R 重型方法多） | 后台跑规避阻塞 | pytest-xdist 并行 / 给 R 慢测打 `@pytest.mark.slow` 分层 / 缓存 R 进程 |
| discover 仍靠离线 SEED | SEED 手写、id 已对齐 catalog | **阶段2**：接真实 CRAN/PyPI/GitHub 抓取（带降级回退离线） |
| ~~无 web 前端~~ ✅ | **已完成（v0.8，1j）**：`web/static/index.html` 单页（上传→画像→清洗→目标筛选→推荐+🟢🟡🔴+6维评分卡+偏差→跑→报告+下载）+ `web/app.py` 全 API（含 /api/run config）+ `cli web` + test_web 9 绿。可用性 58→83 | 续：discover UI / 前端 config 表单（nice-to-have） |
| 评分卡流行/新颖维是离线编辑先验 | scoring.py 规则 | 趋势引擎接通后用真实流行度/更新喂回 |
| BART 样本内 R²（无 CV） | 已披露"偏乐观" | 可加 holdout/CV R² |
| ~~**`run.py` 巨石**：7935 行 / `run_analysis` ~5500 行 / 67 分支 elif~~ | ✅ **已解决（2026-06-16）** | 拆成 15 个 `executor/branches/*.py`（注册表分发，`_branch_api.py`）+ helper 迁 `_helpers/{core,backends}.py`（run.py re-export）。`run_analysis` 只剩 setup+dispatch+teardown。**run.py 7935→148 行、最大模块 1436<1500**；70/70 id 注册、0 缺失、无循环；全量 227 绿 + `test_module_size` 护栏锁定 ≤1500。「prompt too long」元凶根除 |
| 48 处静默 `except Exception: pass` | 多为绘图 best-effort（合规，CLAUDE.md 允许）；0 处裸 `except:`（好） | 抽查**非绘图**的静默吞错（包住 CSV/文件写/估计计算的），至少 `summary.append("…失败")` 或记日志，别静默丢 |
| ~~sharp `rdd` 的 outcome 解析未排除 running 列~~ ✅ | **已修（波③ 1h）**：`_branch_rdd` 加守卫 `outcome = cfg["outcome"] if (in cont and != running) else next(...)`，与 fuzzy_rdd 一致 | — |
| ~~`causal.py` 逼近软上限（1461 行）~~ ✅ | **已提包（波④ 1i）**：`branches/causal/` 13 个一方法一模块（最大 205 行），`pkgutil.walk_packages` 自动注册，丢文件即加方法、不撞护栏/不抢 merge。13/13 注册、module_size + 行为测试绿 | — |

## 环境/装包（本机已装，便于复现）
Py：rdrobust, doubleml, econml, networkx, dbarts(R), pysyncon, factor_analyzer, lifelines, linearmodels, semopy, **girth(IRT), stepmix(LCA/LPA)** ←6-20 装, **prince(CA/MCA), python-louvain(社团)** ←6-22 装。
R：lavaan, QCA, SetMethods, frontier, plm, gstat, spdep, vegan, cna, metafor, mgcv, lme4, qte, JM, ALDEx2, ANCOMBC, dbarts, **mirt(IRT), did(Callaway-Sant'Anna), poLCA(LCA)** ←2026-06-20 装, brms+rstan（**但无编译器、不可用**）。**装 R 包要用 rbridge `find_rscript()` 的路径**（裸 `Rscript` 不在 Bash PATH，会静默失败）。
**缺**：blavaan, JAGS, RTools/C++ 编译器（→ Stan 类方法 + 需源码编译的 R 包当前不可跑）。
**仍缺包→方法暂跳**：合成DiD synthetic_did（R `synthdid` 仅 GitHub 源码、需编译器→跳；可纯 Python 实现但算法重）；de Chaisemartin did_multiplegt（R `DIDmultiplegtDYN` 未装，但 Callaway-Sant'Anna 已用 `did` 覆盖交错 DiD 金标准）。〔已建：IRT(girth)、LCA/LPA(stepmix)、Callaway-DiD(did)、GWR(纯 Python)。〕

## 好点子 / 优化灵感（Good ideas — 多来自双审与建造时的发现）

> 审核/建造时冒出的、值得以后做的点子。不一定现在做，但记下来别丢。

**来自 inference-reviewer 双审的建议（已记、择机做）：**
- ~~**DML 旁加 CV R²**~~ ✅ **已做（#4c，2026-06-19）**：报 nuisance 干扰项交叉拟合(样本外) R²（PLR: E[Y|X]+E[D|X]；IRM: 结果模型 g(D,X)，倾向为分类器不报）；披露为诊断非有效性门槛。inference-reviewer 审「correct as-is」（逐源核对 out-of-fold + ml_l=E[Y|X]）。〔仍可做：交叉拟合 `n_rep>1` 多切分取平均更稳；若将来开放 n_rep 需在 flatten 前 assert n_rep==1 或按 rep 平均；causal_forest 的 holdout/CV R² 仍可加。〕
- ~~**causal_forest**：`frac_significant` 多重比较校正~~ ✅ **已做（#4a，2026-06-18）**：BH/FDR 校正 + `fdr_method=fdr_by` 切换 + 双份披露；SE 从 CI 反推（与 econml.stderr 机器精度一致）。
- ~~**BART**：holdout/CV R²~~ ✅ **已做（#4b，2026-06-18）**：80/20 holdout R²（dbarts x.test），样本内仍报但以 holdout 为准。〔split-share 重要性换 SHAP/permutation 仍可做。〕
- **GAMM**：扩 **非高斯族**（binomial/poisson）；RE 的 p 是近似自由度检验，可注明。
- **meta_regression**：加**亚组分析 / trim-and-fill** 补缺、**多层 meta**（3 层）。
- **joint model**：基线风险可选 **spline-PH**（更灵活）；`event_time` 自动检测可优先**按列名**（time/surv/fu/followup）而非"首个常量连续"。
- **meta / CiC**：小 k（<10）偏倚检验功效不足（已披露）；FE 下 I²/τ² 无意义（已改）。
- **staggered_did（Sun-Abraham，2026-06-18 审）**：① 预趋势可加**联合 Wald 检验**(所有 lead=0，`model.f_test`)作为单一原则性统计量(现为逐 lead p<0.05 标记，无多重校正，已披露功效有限)；② 总体 ATT 现为处理后事件期**简单平均**，可加**按格元样本量加权**(对齐 Callaway-Sant'Anna `aggte(simple)`)作为可选聚合(已披露二者略异)。〔should-fix 的 per-(g,e) 观测加权 + pname 精确匹配已当场应用〕

**方法可运行性验证（用户点子 2026-06-16）：**
- 原则：每个方法都用**真实示例数据**跑通 + 出图，**留代码**作为"可运行"凭证。
- 小 demo（KB 级）：committed + 驱动测试（合成数据常不收敛，真实小样本更稳）。
- 大数据（MB+）：**下载→跑通→作图→删数据留代码**（省空间），验证留痕记本日志（来源+结果，便于复现）。
- 极致版：测试**从 R 包内置数据现取**（gsynth simdata / JM aids…），数据不进仓库、只留代码。

**通用工程灵感：**
- **profiler 增强**：按列名识别设计性角色（事件时间、驱动变量、处理组），减少 RDD/JM/synth 对 config 的依赖。
- **测试提速**：pytest-xdist 并行 + R 慢测分层（`@pytest.mark.slow`）。
- **inference-reviewer 子代理**本身是高价值资产——R 后端真推断几乎每个都能挑出真 bug（本阶段抓了 ~10 个 must-fix），值得保持每个真推断方法都派。
- **评分卡**：趋势引擎接通后，用真实流行度/更新喂回 popularity/novelty 维（现为离线先验）。
- **config 机制**：可做一个 `config schema` 校验 + 友好报错（现各分支自查）。

**全量代码审核发现（2026-06-16, Opus max）：**
- ✅ **头号结构债 = `run.py` 巨石 — 已解决（2026-06-16）**：拆成 15 个 `branches/*.py` 注册表处理器（工具 `_migrate_branches.py` 正则搬体、断言防腐，已用毕删除），run.py 7935→2442 行，全量 227 绿。手法：handler 从 `ctx` 解包同名局部变量后跑原分支体逐字不变（全链审计 67/67 只 mutate 不 rebind，故行为保持）。helper 仍留 run.py、family 模块按需 import。**「prompt too long」根治**：读单族文件即可，不必读整 run.py。
- **静默吞错抽查**（见上工程表）：48 处 `except Exception: pass`，多数是绘图 best-effort（合规），但需抽查包住文件/估计的少数。
- **正向确认**：0 处裸 `except:`；83 个测试文件；自评分卡（86 分）已诚实指出最弱两维 = **可用性 58（无 web 前端，阶段3）** 与 **快速性 62（R 测试慢，pytest-xdist/分层）**——与本审核独立结论一致，二者是当前最高优先优化项。

**Codex 跨族审查补充（2026-06-16，nice-to-have；结构性的已写进 roadmap）：**
- **config schema 前移为 v0.9 门槛**（非横切）：每 analysis entry 一份机器可读参数规格,Web UI/推荐解释/运行错误共消费,别各自猜。
- **discover 趋势分 ≠ 包热度**:加 领域归一化 / 维护状态 / 最近发布 / 引用·教程信号 / license·安装可行性,保留人工 gate。
- **Web MVP 要薄**:上传→推荐→config→运行→报告即可;把 schema/错误提示/产物浏览做扎实 > 追 UI 完整度。
- **实验设计要"设计感知"**:RCBD/split-plot/nested/repeated 做成强制声明 block/plot/subplot 角色的 mixed-model wrapper（已入 roadmap）。
- Codex 审查任务书在 `docs/codex-review-brief.md`（之前+接下来的阅读清单 + 红线）。

**中介/调节扩展 + Web 表单 + config 回填（2026-06-23，方法波 + 2 subagent）：**
- **mediation_extra 冷审 ZERO MUST-FIX**（serial/parallel/moderated_moderation）：reviewer 逐位核 path index/serial 分解 a1·d21·b2/3 阶条件斜率 L 向量+解析 SE/df/中心化。
- ✅ **mediation_extra BCa bootstrap CI 已上线（2026-06-24，估计层打磨波）**：serial_mediation（via_m1/via_m2/serial/total）**和** parallel_mediation（每个特定间接 + total + 成对对比）的 CI 从百分位法换成 **BCa**（Efron 1987）。helper `_bca_ci(boot, obs, jack)`：z0=Φ⁻¹(#{boot<obs}/B)（tie-aware 均秩 + 夹 1/2B 防 ±inf），加速度 a 从**同一统计量的 jackknife**（n 次 LOO 重拟合，serial 4 统计量/parallel 每个间接+total）算 a=Σ(θ̄−θ̂_i)³/(6·[Σ(θ̄−θ̂_i)²]^1.5)，调整百分位 α1/α2=Φ(z0+(z0+z_q)/(1−a(z0+z_q)))，z_q=Φ⁻¹(.025/.975)；degenerate（jackknife 常数 / <3 个不同 bootstrap 值）→回退百分位。复用既有 bootstrap 抽样做百分位输入，只新增 jackknife 遍历。⚠ 文案改"bootstrap BCa 95% CI"；n≥30 守卫保留；点估计/total=Σspecific 恒等不受影响。**剩 NICE**：B=2000→5000（Hayes 建议、已披露）；serial/parallel 可报 c' 的 CI。
- **Web 动态 config 表单（subagent A，已集成）**：`web/static/index.html` 据 recommend payload 的 `params` 渲染表单（column→列下拉、columns→多选、choice→选择、int/float→数字、bool→勾选），空字段省略→回退默认，空 params 提示按默认跑。仅前端，test_web 9 绿。
- **config params 回填（subagent B，已集成）**：47 文件/165 entry 加 `params:`（共 177 entry 有 params）。主脑集成时**修了 2 个未引号化的 `{}` 描述 YAML 崩**（categorical_tests `{类别:概率}`、survey_methods `{var:{level:prop}}`）+ **补 12 个 under-declare 真键**（field_trials outcome 别名、MCDA 6 个 weights、power_analysis environment/genotype、irt_pcm group、kruskal within/subject/conditions）防假警告；写了 within-file 完整性校验脚本（declared⊇read，仅剩 croston seasonal_periods 一个确认的假阳=模块广播但不实读）。**回填 backlog**：跨文件 resolver 键（_helpers 的 _regression/_io_names 等）未逐一校验，残留少量假警告风险（非阻塞）；可把完整性校验做成 CI guard。

**config params 完整性 CI guard（2026-06-24，subagent B + 主脑修）：**
- ✅ `tests/test_config_params_complete.py`：每个声明 params 的 entry，其 handler（含**模块内 helper 调用图**，仅 _helpers/ 跨文件共享）读的 cfg 键必须 ⊆ 声明的 params 名（防 under-declare 假警告回归）。informational 测另列「读 cfg 但未声明 params」的回填候选（不 fail）。
- **主脑集成修 subagent B 两个 bug**：① helper 索引原按名跨**所有 branch 模块**取并集→`_resolve_xy` 在 spatial_extra(读 x/y) 与 ml_supervised(读 outcome/predictors) 撞名→svm 误判读 x/y；改为**模块内 + _helpers/ 全局**解析；② `_declared_params` 原过滤 `executor_ref.startswith("py::")`，但 executor_ref 是自由字符串(numpy/semopy/…)→ topsis 等被误判「declares []」；改为映射全部 entry。
- **guard 抓到 3 个真漏**（前次手工回填漏的）：getis_ord/join_count/spatial_panel 读 `knn_k` 未声明→已补（用户传 knn_k 本会得假「未知参数」警告）。**现 183/219 entry 有 params，guard 全绿。**

**设计感知田间试验冷审（2026-06-23，field_trials：rcbd/latin_square/split_plot）—— ZERO MUST-FIX：**
- 冷启动 inference-reviewer **逐位核验**：split-plot SS 分解对齐 statsmodels 全模型 Type-II（<1e-6，含 m=2 行洗牌→证明 map/merge 按标签对齐非行序）、误差层路由（主区 A 对区组×A 误差、df=(a-1,(r-1)(a-1))，非裸 MS_A/MS_sub）、latin 残差 df=(t-1)(t-2)、RCBD 相对效率 Fisher 公式、角色集合运算优先级——全对。
- ✅ **SHOULD-FIX 已修**：latin_square 原仅校验边际(t==row==col、n≥t²)，非真正拉丁方(如处理混叠到列、n==t²)会被静默当正交分析。已加真拉丁方校验（t² 不重复单元/每单元一次/每处理每行每列各一次，否则诚实跳过）+ 测试。
- ✅ **NICE 已做**：split-plot summary 加复合对称假定披露（裂区水平>2 指向 MixedLM/GG）。
- ✅ **NICE 已做（2026-06-24，披露波）**：rcbd/latin summary 各加一句 η²/偏η² 小设计上偏、ω²/ε² 偏差更小的 ⚠。（仍可后续补算 ω² 数值。）split-plot 不平衡已诚实跳过指向 MixedLM；将来可接 MixedLM 通用协方差版裂区。

**v0.9 config schema（2026-06-23）—— infra 已做 + 回填 backlog：**
- ✅ **已做**：`ParamSpec` 模型 + `AnalysisEntry.params`；`catalog/config_schema.py` `validate_config`（未知键/坏 choice/列不存在/类型错，**非阻塞**警告）；`run_analysis` 接入（⚠ 进 summary）；`cli params <id>`；recommend payload 带 `params`；correlation_suite/effect_sizes 两族填好范例；`tests/test_config_schema.py`。
- **回填 backlog（~30 族 params 待声明）**：逐族在 yaml 加 `params:` 块。**方法**：审 `@register` handler **及其调用的 helper** 的全部 `cfg.get` 键（共享 resolver 的键，如 forecasting 的 column/value、effect_sizes 的 outcome/group，常在 helper 里、单看 handler 体会漏）；**宁可不填也别 under-declare**（漏键会对合法 config 发假"未知参数"警告）。数据源 = loop-decisions 各行「config 键」列。可写个一次性脚本：正则抽每族每 handler+helper 的 cfg.get 键 → 生成 params 草稿 → 人核类型/描述。
- **Web 前端表单**：payload 已带 params，但前端尚未据此渲染 config 表单（A 版 web 仍是固定/无表单）。补全：前端用 `rec.params` 动态生成输入控件（column→列下拉、choice→选择框、float/int→数字框），提交进 `/api/run` 的 config。

**v0.9 脏数据鲁棒（2026-06-23，`profiler/ingest.py` 鲁棒读取门）—— 已做 + 刻意 defer 的 i18n 尾巴：**
- ✅ **已做**：编码回退(utf-8-sig→gb18030→latin-1)、分隔符嗅探(`,;\t|`)、保守数值强转(文本"1,234"/"$5"/"12%"/杂缺失标记 ≥90% 可解析才转、`df.attrs['rf_coercions']` 非静默)、`diagnose` 加 coerced_numeric/high_cardinality 披露、12 脏数据测试。
- **i18n 小数逗号**（European `1,5`=1.5）：刻意**不**处理——千分位正则只吃"逗号+恰好3位数+边界"，`1,5` 解析失败→留文本（保守，宁可不转也不把 1,5 错成 15）。补全：检测整列单逗号无小数点模式→按小数逗号解析。
- **charset 检测器**：编码链是静态启发式（法语 latin-1 文件可能先被 gb18030 错解为中文乱码再到 latin-1）。补全：可选接 `charset_normalizer`（纯 Py、常随 requests 装）做最佳猜测，缺则回退静态链。
- **不规则行容错**：现用 C parser 默认；ragged/坏行会抛 ParserError。补全：捕获后用 `engine="python"` + `on_bad_lines="warn"` 重试（别静默丢行）。
- **code 产物可复现性**：`run.py` 导出的代码片段仍是裸 `pd.read_csv(path)`，若发生了强转则无法逐位复现。补全：强转发生时在导出代码里带上 ingest 的清洗步骤（或导出 `from researchforge.profiler.profile import read_table`）。

**波⑦ 6 方法族冷审（2026-06-23，6 个 inference-reviewer；4 ZERO / 2 MUST-FIX 已当场修）：**
- ✅ **MUST-FIX 已修**：① correlation_suite 偏相关 Fisher-z CI off-by-one（`_fisher_ci(dof+3)`→`dof+2`，SE 1/√(n−k−2)→正确 1/√(n−k−3)，k=0 时正确退回零阶 1/√(n−3)）；② time_series_diagnostics 阶数提示**标签**说"末位显著滞后"但代码实为 Box-Jenkins 截断（初始连续显著段）——summary+yaml 已对齐（数值一直对，纯诚实标签 bug）。
- ✅ **effect_sizes 披露已补（2026-06-24，披露波）**：d/g summary 加 ⚠ 大样本正态近似 CI 小 n 欠覆盖、偏窄；cohens_d 加 ⚠ CLES=非参 Mann-Whitney AUC（非 Φ(d/√2)，非正态下二者不必相等）+ Glass's δ 对照=数据第二个出现水平的披露。
- ✅ **effect_sizes 非中心 t 精确 CI 已上线（2026-06-24，估计层打磨波）**：cohens_d **和** hedges_g 的 CI 由大样本正态近似换成**非中心 t pivot 精确区间**（Cumming 2012 / Steiger-Fouladi 1997）。helper `_noncentral_t_ci`：`t_obs=d·√(n1n2/(n1+n2))`，df=n1+n2−2，用 `scipy.stats.nct.cdf` + `scipy.optimize.brentq`（含 cdf-随-λ-递减的自适应 bracket 搜索）反解 λ_hi(α/2)/λ_lo(1−α/2)，乘 `√((n1+n2)/(n1n2))` 换回 d 尺度；Hedges' g 的 CI = J × d 的非中心 t CI（g=J·d 单调缩放）。**收敛守卫**：bracket/brentq 失败→回退正态近似 `_normal_smd_ci` 并在 summary 诚实标注"精确法未收敛已回退"。⚠ 文案改为"非中心 t 精确区间"（删掉"正态近似欠覆盖"告警，因已不适用）；ci 置信水平仍由 `config["ci"]` 控。**剩待办**：加 `config["control"]` 让 Glass δ 可指定对照组。
- ✅ **dimensionality_extra 已修（2026-06-24，披露波）**：t-SNE 轮廓改在标准化输入 `Xs` 上算（不再用嵌入，标签+披露改为「输入空间可分性描述」，test_tsne 仍绿）；FA 加 Heywood/低共同度条件 ⚠（min<0.2 或 max>0.98 才触发，从已有 communalities 算）；FA/LDA 各加载荷/判别轴符号任意性 ⚠；total_var/var_ld1/cv_acc/KL 的 NaN 插值加 N/A 守卫（显示，不再 "nan%"/"nan"）。
- ✅ **forecasting exponential_smoothing 模型一致 PI 已上线（2026-06-24，估计层打磨波）**：`exponential_smoothing` 的预测区间从手搓 `z·σ·√step`（SES 律）换成 **statsmodels 状态空间 ETS** 区间——`statsmodels.tsa.exponential_smoothing.ets.ETSModel`（沿用 HW 拟合的同一 trend/seasonal/seasonal_periods，error 据 mul/add 自适应）+ `res.get_prediction(start=n, end=n+h-1).summary_frame(alpha=0.05)` 取 mean/lower/upper（lower/upper 列名跨版本嗅探）。点预测/α/β/γ/AIC/SSE 仍来自原 HW 拟合（保持 estimates 不变），仅 PI 改模型一致区间。**诚实降级**：ETSModel import/fit 失败或产生非有限值→回退原 √step 近似 + summary 披露"状态空间 ETS 区间不可用，已回退近似"。⚠ 文案更新为"状态空间 ETS 模型一致区间"。**剩待办**：theta 仅点预测（可加残差自助 PI）；croston 间歇阈值硬 25%（可换 CV²/ADI 象限分类）。
- ✅ **correlation_suite 披露已补（2026-06-24，披露波）**：correlation_matrix summary 加 ⚠ n=完整个案数（complete-case / listwise 删除），某列多缺会同时拉低每一对的 n。
- ✅ **interpretability ALE 空箱处理 + 3 披露 NICE 已上线（2026-06-24，估计层打磨波）**：① **accumulated_local_effects 空箱**改按标准 ALEPlot 约定——空分位箱**整段跳过**（不贡献局部效应、不在累积曲线注入伪 0 斜率节点），仅保留非空箱的局部差分序列与其边界节点，count 加权中心化仅用非空箱（连续数据上分位箱罕空、行为与旧实现一致，ALE 测试全绿）；② **feature_interaction** 加 ⚠：联合部分依赖幅度极小（Σpjk²<1e-6）的特征对 H=两微小量之比、数值不稳/噪声大，逐对点名；③ **quantile_intervals** 加 ⚠：经验覆盖率来自单次随机留出（test_size=0.25, random_state=0，列出 n_test），有抽样噪声、换种子/CV 会波动；④ **feature_interaction** 当 config 特征数 >4 时 summary 注明只用模型重要性最高的前 4 个（列出实际所用）。
- **categorical_tests（nice-to-have）**：估计 dict 里 p_value round 到 0.0（summary `:.4g` 仍显小值，dict 可保精度/设 ε 下限）；无 `config["mode"]="gof"` 强制 GoF 钩子（多列时只能跑独立性）。
- **spatial_panel（SAR/SEM/SDM, splm）impacts 的 SE**：当前 direct/indirect/total 用 LeSage-Pace **解析点估计**（exact，`S=(I-ρW)^{-1}(Ib+Wθ)`），**未附模拟标准误/CI**。原因：splm 自带 `impacts()` 在本机 spdep/spatialreg 版本下易碎（`trW`、`as_dgRMatrix_listw` 已移位/改名，且 `impacts(spml)` 报 `have_factor_preds` 断言失败），故绕过自带实现、自算点估计。**补全**：用 ρ、β 的协方差（`vcov(m)`）做 delta-method 或参数自助（draw ρ/β ~ N(est, vcov)，重算 S，取分位）给 impacts 的 SE/CI。
- **spatial_panel 的 W 仅 k-NN**：默认行标准化 k-NN（k=6，欧氏经纬度）。**待优化**：可配距离阈值/contiguity/反距离权重，以及真测地距离（与截面 spatial_regression 同一 backlog 第 6 条）。
- **spatial_panel FE 仅个体（within, individual）**：未做双向（个体+时间）或随机效应空间面板，也未自动跑 Hausman 选 FE/RE-spatial。**补全**：接 `effect="twoways"` 与 `spml` 的 RE 变体 + `sphtest`（空间 Hausman）。

---
*持续追加。受硬件/装包限制绕过的、以及审核时的好点子，都在此留痕。*
